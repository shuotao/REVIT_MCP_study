const fs=require('fs');let c=fs.readFileSync('MCP/Core/CommandExecutor.cs','utf8');let st='        /// <summary>\r\n        /// 取得目前選取的元素';let ed='Message = $\成功在 {pipe.Name} 安裝 {symbol.Name}\\r\n                };\r\n            }\r\n        }\r\n\r\n';let count=0;while(c.includes(st)){let s=c.indexOf(st);let e=c.indexOf(ed,s);if(e===-1)break;c=c.substring(0,s)+c.substring(e+ed.length);count++;}fs.writeFileSync('MCP/Core/CommandExecutor.cs',c);console.log('Removed '+count);
