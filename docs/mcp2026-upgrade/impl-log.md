# MCP 2026 Upgrade Implementation Log

- [step PR1-CI] check-pr.yml scoped to fork PRs | attempt 1 | pass=true buildOk=true | All four QAQC criteria pass. (a) check-pr.yml is valid YAML — verified by actually parsing it with js-yaml@4 (installed into an isolated scratchpad since no YAML lib was preinstalled); yaml.load succeeded and produced jobs.check-files with exactly 2 steps. (b) Fork-only gating correctly uses head.repo.fork: enforcement step `if: github.event.pull_request.head.repo.fork == true`, skip/info step `if: ...fork != true` (complementary and correct). (c) Fork-PR behavior unchanged — the fork-gated github-script step retains the identical guardrail (allowedPaths=['domain/','GEMINI.md'] + core.setFailed on any disallowed file), so fork PRs touching code still fail; no duplicate unconditional step exists. (d) CONTRIBUTING.md note present at line 66 clarifying the knowledge-only limit applies only to external fork PRs while maintainer same-repo PRs may modify code. No git commands were run.
- [step WP-B+D] tool annotations + deterministic sort | attempt 1 | pass=true buildOk=true | Build clean (tsc, 0 errors). registerRevitTools() -> count=167, missing=[], sorted=true, destructive=["dedup_detail_elements_in_view","delete_element"]. All four PASS criteria met (the template's "count==undefined" resolves to the CLAUDE.md source-of-truth count of 167, which matches). Every tool has a non-empty title and boolean readOnlyHint/destructiveHint; no empty titles; no misclassified defaults. withAnnotations wiring confirmed on both return paths (unknown-profile fallback + normal) with .flat().map(withAnnotations).sort(localeCompare); MCP_PROFILE logic and console.error banner untouched.
- [step QAQC-9-1] verify-qaqc.ps1 Phase 9-1 | attempt 1 | pass=true buildOk=true | Verified scripts/verify-qaqc.ps1 against QAQC discipline (no git run). (1) PSParser::Tokenize returned PARSE_OK — script parses cleanly. (2) Phase 9-1 correctly references registerRevitTools (line 969, mirroring Phase 7's Get-ToolCount), the destructive allow-list appears both Node-side (new Set(['delete_element','dedup_detail_elements_in_view']), line 969) and PS-side ($destructiveAllowList, line 992), and it uses only the existing Write-Check helpers (lines 997/1000/1003) plus Write-Skip fallback (line 989) — no new output helpers. (3) No renumbering: Phase 7 (line 512) and Phase 8 (line 890) headers unchanged; Phase 9 is genuinely new (line 958); header comment block keeps 1-8 intact and appends line 13. (4) Live run pwsh -File scripts/verify-qaqc.ps1 -SkipBuild -SkipDeploy: Phase 9-1 printed three PASS lines against the 167-tool runtime registry (non-empty title / boolean readOnlyHint / destructiveHint confined to allow-list). Overall PASS:58 FAIL:0 WARN:0 SKIP:3, RESULT PASSED, exit 0. SKIPs are the expected build/deploy phases; 167 matches CLAUDE.md's stated count.
- [step DOCS] MIGRATION_GUIDE dual-era note | attempt 1 | pass=true buildOk=true | Verified MCP-Server/docs/MIGRATION_GUIDE.md contains the new section "🔀 MCP 2026-07-28 雙時代相容升級" with all 5 points present as ### 1️⃣–5️⃣ headings (additive/backward-compat; title+readOnlyHint/destructiveHint metadata; deterministic tools/list sort; wire-level features DEFERRED; fork contribution policy unchanged) plus a Q&A block. Markdown is valid: clean heading hierarchy #→##→###→#### with no skipped levels. No broken links — grep found zero [text](url) link syntax; all references are inline code spans and every referenced path exists on disk (CONTRIBUTING.md, .github/workflows/check-pr.yml, MCP-Server/src/tools/index.ts, impl-log.md). MCP-Server/docs/mcp2026-upgrade/impl-log.md exists with 3 earlier-step entries ([step PR1-CI], [step WP-B+D], [step QAQC-9-1]). No git commands run.
