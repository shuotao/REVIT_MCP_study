# MCP 2026 Upgrade — Documentation Alignment Log

- [step DOC-ALIGN] Align repo docs with v1.6.0 (MCP 2026-07-28 Path-A dual-era + WP-F Apps clash-viewer + SDK 1.30) | attempt 1 | pass=true buildOk=n/a (docs-only) | Scope: CLAUDE.md, README.md, README.zh-TW.md, docs/DOCUMENT_AUDIENCE_INVENTORY.md. No files under MCP-Server/src, MCP/, or scripts/*.ps1 logic were touched; scripts/verify-qaqc.ps1 was read-only reference, not modified. Changes: (1) CLAUDE.md "Key Source Files" gained rows for `MCP-Server/src/tools/annotations.ts`, `MCP-Server/src/apps/register-apps.ts`, `MCP-Server/src/apps/clash-viewer/`, `MCP-Server/scripts/build-apps.mjs`; "Build Commands" gained a note that `npm run build` = `tsc && node scripts/build-apps.mjs` and now also produces `MCP-Server/build/apps/*/index.html`; new "## MCP Protocol Posture (2026-07-28 Dual-Era)" section summarizes annotations, deterministic `tools/list` order, MCP Apps resources capability + clash viewer, SDK 1.30, and the deferred wire-level feature list, pointing to `docs/MIGRATION_GUIDE.md`; "QA/QC" bullet list gained a Phase 9 line (9-1 annotation coverage, 9-2 Apps `ui://` resource integrity). Source-of-truth counts (tools=167, domain=74, skills=50) were left untouched, matching the runtime truth confirmed by a live `verify-qaqc.ps1` run before editing. (2) README.md and README.zh-TW.md each gained a short "New: Interactive Clash Viewer (MCP Apps)" section stating the feature needs a ui-capable host, is additive, and that stdio + the existing Revit add-in are unchanged; both also had a stale "166 MCP tools" hero-line corrected to "167" to match each file's own count table (pre-existing drift, not caught by any Phase 7 claim-site regex since the English/Chinese phrasing there didn't match a tracked pattern; fixed while touching the paragraph for count-consistency, no other counts changed). (3) docs/DOCUMENT_AUDIENCE_INVENTORY.md classified the three new/updated docs: `docs/MIGRATION_GUIDE.md` added to the Human-Facing Documents table (Traditional Chinese, users + fork contributors); `docs/mcp2026-upgrade/impl-log.md` and this file added to the AI-Only Documents table as "implementation audit log" (English, append-only, not onboarding content). Verification: `pwsh -NoProfile -File scripts/verify-qaqc.ps1 -SkipBuild -SkipDeploy` run after all edits — 0 FAIL, 0 WARN, 3 SKIP (build/deploy phases only), counts confirmed tools=167 / domain=74 / skills=50, Phase 7 count-exact-match / link-rot / audience-inventory-exists checks and Phase 8 mojibake checks on CLAUDE.md + both READMEs all PASS. No git commands were run.

## Round 1 — Sonnet align
- **CLAUDE.md**: Added Key Source Files rows (annotations.ts, register-apps.ts, clash-viewer/, build-apps.mjs), Build Commands note on tsc/build-apps.mjs, MCP Protocol Posture section, Phase 9 QA/QC coverage
- **README.md**: Added Interactive Clash Viewer section, fixed tool count 166→167
- **README.zh-TW.md**: Added Traditional Chinese Clash Viewer section (互動式碰撞檢視器), fixed tool count 166→167
- **DOCUMENT_AUDIENCE_INVENTORY.md**: Added MIGRATION_GUIDE.md to Human-Facing docs, impl-log.md and doc-alignment-log.md to AI-Only audit logs

### Round 1 — OPS verdict
- pass=true
- verify-qaqc=59 PASS / 0 FAIL / 0 WARN
- issues=[]
