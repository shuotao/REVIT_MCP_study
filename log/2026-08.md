# Event Log — 2026-08

> Append-only project development log.

## [2026-08-03 19:31] kanban | 將 TASK-005.x 重整為 11 種 Set 導入 Revit 情境卡
- actor: GPT-5 (via Codex)
- files: tools/kanban/cards/TASK-005.1.json ~ TASK-005.11.json, tools/kanban/cards/TASK-008.json, tools/kanban/cards/TASK-009.json, kanban.html, tools/kanban/index.html
- trigger: manual
- summary: 依 TASK-003 Master Mapping 將 TASK-005 子卡改為情境 1～11；情境 1、10 設為 verify，其餘設為 ready，並同步上層驗證卡及兩份看板頁面。

## [2026-08-04 02:28] archive | organize green-material Revit development assets
- actor: GPT-5 (via Codex)
- files: tools/green-material/**, docs/green-material/**, generate_revit_injection_plan.py, .claude/skills/GMimport/SKILL.md, README.md, README.zh-TW.md, docs/DOCUMENT_AUDIENCE_INVENTORY.md
- trigger: manual
- summary: Classified historical green-material scripts, intermediate data, reports, and snapshots while preserving established runtime entry points and MCP source locations.

## [2026-08-06 17:30] feat | Set manager UI additions, layerComposition-aware plan engine, v5 six-slot shared-parameter schema
- actor: claude-sonnet-5 (via Claude Code)
- files: assets/green-material-showcase.html, .claude/skills/GMweb/SKILL.md, generate_revit_injection_plan.py, GreenMaterial_SharedParams.txt, MCP/Core/Commands/CommandExecutor.GreenMaterial.cs, MCP-Server/src/tools/visualization-tools.ts, MCP-Server/src/tools/base-tools.ts, domain/green-material-parameter-schema.md, .claude/skills/import/SKILL.md, tools/kanban/cards/TASK-013.json, tools/kanban/cards/TASK-014.json, tools/kanban/cards/TASK-015.json, kanban.html, tools/kanban/index.html
- trigger: manual
- summary: Added "add to existing Set", project-file export/import, and a draggable material-layer (Structure/Substrate/Finish/Core Boundary) UI to the showcase page plus `/GMweb open`; made the injection-plan engine honor `layerComposition` and assign Mat slots deterministically (`_assign_material_slots`); expanded GreenMaterial_SharedParams.txt from v4 (Mat1-3, 31 fields) to v5 (Mat1-6, 64 fields) across C#/MCP-tool/Python, rebuilt and redeployed the Revit 2024 add-in, verified end-to-end in a test document, and fixed a Mat-slot-order-vs-physical-layer-order mixup in the `/import revit` skill instructions. Kanban regenerated from `tools/kanban/cards/*.json` (also picked up 7 previously-unsynced TASK-005.x cards).

## [2026-08-07 10:15] feat | Add /GMupdate and /GMset compare skills for refreshing and diffing TABC source data
- actor: claude-sonnet-5 (via Claude Code)
- files: update_tabc_database.py, generate_revit_injection_plan.py, .claude/skills/GMupdate/SKILL.md, .claude/skills/GMset/SKILL.md, tools/green-material/README.md, CLAUDE.md, README.md, README.zh-TW.md, docs/DOCUMENT_AUDIENCE_INVENTORY.md, docs/BIM_MCP/index.html, docs/BIM_MCP/reference/skills-index.html
- trigger: manual
- summary: There was previously no way to refresh `tabc_master_database.json` from the live TABC site or to check whether an existing material Set had drifted from current source data. Added `update_tabc_database.py` (GBMTYPE 1-4 list-page crawl, merge-not-overwrite so a partial crawl never wipes real records, re-syncs `assets/green-material-showcase.html`'s embedded offline cache) driven by the new `/GMupdate` skill, and `compare_all_sets()`/`compare_and_refresh_set()` in `generate_revit_injection_plan.py` (diffs a Set's licnos against the current master DB for missing/expired/renamed materials, using the licno→title snapshot embedded in each Set's `purpose` text since `Revit_Injection_Plan.json` itself is overwritten on every run) driven by the new `/GMset compare` skill — refresh only touches plan/report files, never Revit directly. Verified both live against the real TABC site (dry-run crawl succeeded, compare ran cleanly against the current Set). Bumped the Claude skills count 54 -> 56 across CLAUDE.md/README.md/README.zh-TW.md/docs/DOCUMENT_AUDIENCE_INVENTORY.md/docs/BIM_MCP site pages per the count-table gate.

## [2026-08-07 10:45] fix | Green-material keyword mention no longer auto-opens the showcase webpage
- actor: claude-sonnet-5 (via Claude Code)
- files: domain/green-material-catalog.md, domain/green-material-keyword-search.md
- trigger: manual
- summary: `domain/green-material-catalog.md` (§5, §5.1, §6) and `domain/green-material-keyword-search.md` (§0, §2) previously instructed the AI to automatically pop open `assets/green-material-showcase.html` the moment a green-material keyword was mentioned in conversation. Changed both to ask the user first and only run `/GMweb open` after confirmation (skip the ask only when the user's message is already an explicit open request). Also fixed §5.1/§6 in `green-material-catalog.md`, which still described writing per-search-result JSON directly into the showcase HTML's dynamic library — that's stale versus the actual `/GMupdate` (`update_tabc_database.py`) merge-based refresh mechanism added earlier today; both now point at `/GMupdate` instead. The equivalent policy was also added to the ported `green-material-search-toolkit` skill in the separate HJPLUS_Taiwan_Architect_KB clone (its own `SKILL.md`, new "Trigger Confirmation Rule" section) so both projects behave the same way.

## [2026-08-07 11:05] fix | Remove duplicate /GM search branch (redundant with /GMweb open)
- actor: claude-sonnet-5 (via Claude Code)
- files: .claude/skills/GM/SKILL.md, .claude/skills/GMweb/SKILL.md, .claude/skills/GMset/SKILL.md, .claude/skills/GMupdate/SKILL.md, docs/BIM_MCP/reference/skills-index.html
- trigger: manual
- summary: `/GM search` and `/GMweb open` opened the exact same page via identical steps (check port 8888, start local_server.py if needed, open browser) — a pointless duplication. Narrowed `/GM` to kanban-only (its `search` action and all port/server logic removed); `/GMweb open` is now the single entry point for the green-material search & Set Manager page. Updated cross-references in GMset/GMupdate SKILL.md and the skills-index.html doc page accordingly. Skill count unchanged (56) — no file was deleted, `/GM` was narrowed in place.

## [2026-08-11 00:46] feat | TASK-005.2 — set_material_surface_pattern MCP tool for Floor tile/wood Surface Pattern
- actor: claude-sonnet-5 (via Claude Code)
- files: MCP/Core/Commands/CommandExecutor.GreenMaterial.cs, MCP/Core/CommandExecutor.cs, MCP-Server/src/tools/visualization-tools.ts, .claude/skills/import/SKILL.md, tools/kanban/cards/TASK-005.2.json, kanban.html, tools/kanban/index.html, CLAUDE.md, README.md, README.zh-TW.md, docs/DOCUMENT_AUDIENCE_INVENTORY.md, docs/BIM_MCP/reference/skills-index.html, docs/BIM_MCP/reference/personal-llm-wiki.html, docs/BIM_MCP/reference/philosophy-22-propositions.html, docs/BIM_MCP/reference/domain-index.html, docs/BIM_MCP/reference/architecture-v2.html
- trigger: manual
- summary: Started TASK-005.2 (地磚與表面填充 Set 導入). Floor Type duplication, Substrate/Finish1 thickness defaults (15mm/20mm), and shared-parameter binding were already covered generically by TASK-005.1's `create_multi_layer_type`/`create_single_material_type`/`load_shared_parameters` tools — the one genuinely missing capability was the acceptance criteria's Surface Pattern requirement (600×600 grid for tile, wood-grain for flooring). Added `set_material_surface_pattern` (new C# method + dispatcher case + TS tool definition): builds or reuses (dedup by FillPatternElement name) a Model-target `FillPattern` — a 0°/90° grid for tile joints or a single-direction offset line pattern for wood grain — and assigns it to a Material's `SurfaceForegroundPatternId`/`CutForegroundPatternId`. Wired it into `/import` skill Scenario 2 (step 5b) and Scenario 3 (step 6b) so Floor finish-layer materials get a pattern applied right after Type creation. Verified via `dotnet build -c Release.R24` (0 errors) and `npm run build` (MCP-Server, clean). Runtime tool count 175 -> 176, synced across CLAUDE.md/README/README.zh-TW/audience inventory/BIM_MCP reference pages. Kanban card moved ready -> implementing (architecture gate now true); test/code_review gates remain open pending an actual Revit end-to-end run with a real tile Set, which needs a live Revit connection and user confirmation per the skill's mutation-confirm rule — not run in this session.

## [2026-08-11 01:00] feat | TASK-005.2 end-to-end Revit run via /GMimport -> /import revit (Set 地磚test)
- actor: claude-sonnet-5 (via Claude Code)
- files: exported_material_sets.json, Revit_Injection_Plan.json (generated, not tracked), tools/kanban/cards/TASK-005.2.json (content only, superseded by the 01:23 reorg entry below)
- trigger: manual
- summary: Ran `/GMimport` (Set 【地磚test】, GBM0104033 透水性混凝土地磚, Floor/單一組合, 1 material — no separate substrate licno) then `/import revit`. Deployed the new `set_material_surface_pattern` DLL to `%APPDATA%\Autodesk\Revit\Addins\2024\RevitMCP\` (Revit was confirmed running 2024) and confirmed via a live `get_project_info` re-anchor each step. Hit two full data-loss cycles where the Revit document was closed without saving between steps (verified via `get_all_materials`/`get_types_by_category` returning empty each time) — rebuilt the Floor Type from scratch both times before the user confirmed a real save. Final successful run: `create_multi_layer_type` built `TABC_地磚test` (Element ID 264193, source TypeId 339) with Finish1 GBM0104033_透水性混凝土地磚 (Material ID 264195, 15mm, user-confirmed via AskUserQuestion) + Substrate 1-3水泥砂漿打底 (Material ID 264197, non-green generic material, 20mm, added after asking the user since the Set had no licno for the mortar layer); `set_material_surface_pattern` applied `TABC_Grid_600x600` (Pattern ID 264214) to the tile material's Surface pattern — first live use of the new tool; 13/13 shared parameters written with zero `MissingParameters`, verified via `get_element_info`; `exported_material_sets.json` updated to `已完成 Revit 牆體元件注入`. TASK-005.2 moved to `verify` stage (see 01:23 kanban reorg entry for the card content update).

## [2026-08-11 01:23] chore | Kanban reorganization — merge overlapping infra cards, fix broken dependency, re-derive order sequence
- actor: claude-sonnet-5 (via Claude Code)
- files: tools/kanban/cards/TASK-005.2.json, TASK-005.9.json, TASK-005.10.json, TASK-009.json, TASK-010.json, TASK-011.json, TASK-012.json, TASK-007.json, TASK-008.json, TASK-001/002/003/004/005.1/005.3-005.8/005.11/006/013/014/015.json (order field only), tools/kanban/index.html, kanban.html
- trigger: manual (user: "看板有點亂...重工的任務...請幫我重新整理一遍，並重新排序")
- summary: User flagged the kanban as cluttered with duplicate work. Root cause: two independent "情境/scenario" numbering schemes collided — the original 11-scenario planning cards (`TASK-005.1`~`TASK-005.11`, created 08/03) and the `/import` skill's internal Scenario 1/2/3 path names, which later became their own dev cards (`TASK-009`/`010`/`011`, built 08/03-08/04) describing the *same* underlying tool work (duplicate_element_type for TASK-005.1, create_single_material_type for TASK-005.10's Floors path, create_multi_layer_type for TASK-005.9's soundproof case and TASK-005.10's Floors multi-material path). Also found a broken `dependsOn: ["TASK-005"]` on TASK-007 (no such card ID exists, only 005.1-005.11 sub-cards) and TASK-008's progress notes stale relative to actual tool state. Per user's confirmed "完整整併" (full merge) choice: marked TASK-009/010/011 `done` with in-content merge notes pointing to their target 005.x card (kept, not deleted, for git-traceable history — TASK-010 explicitly noted as cross-cutting infra not owned by any single 005.x card, to avoid overclaiming); ticked TASK-005.10's Mat3/Floors-multi-material/traceability criteria with evidence citations (Ceilings case and final review still open); added a partial-evidence note to TASK-005.9 without falsely ticking its Wall/insulation-still-missing criteria; marked TASK-012 (resolved "forgot to save" incident) `done` with a note that future incident write-ups belong in `log/` rather than a kanban card; fixed TASK-007's dependency to point at TASK-005.1/TASK-005.2 instead of the dangling ID (avoided a 007<->008 cycle); updated TASK-008's scenario-by-scenario progress list to reflect 005.1/005.2 now complete and 005.9/005.10 partially evidenced. Re-derived every card's `order` field into a single Epic-grouped sequence (Epic-1: 1-3, Epic-2: 4-8.97 with 005.x sub-scenarios at 8.1-8.96 and merged infra cards slotted at collision-safe decimals like 8.11/8.91/8.955, Epic-3: 10, Epic-4: 11-12), replacing the old scheme that interleaved Epic-1/3/4 whole-number orders (6,7,8,13) in between the Epic-2 005.x decimal block. Regenerated the `cardsData` embed in both `kanban.html` and `tools/kanban/index.html` from the 25 updated card files via a one-off Python script (CRLF-preserving), verified both embeds are byte-identical and still valid JS (`node -e` eval parse, 25 unique IDs). No cards deleted.

## [2026-08-11 01:48] feat | TASK-005.4 — auxiliary-material drop zone in the layer-composition modal + plan-engine wiring
- actor: claude-sonnet-5 (via Claude Code)
- files: assets/green-material-showcase.html, generate_revit_injection_plan.py, tools/kanban/cards/TASK-005.4.json, kanban.html, tools/kanban/index.html
- trigger: manual (user showed a screenshot: the 材料層級設定 modal was forcing GBM0104110, a sealant/填縫劑, into the "Structure（結構層）" dropdown because the modal only offered Structure/Substrate/Finish — no place for non-geometric auxiliary materials)
- summary: TASK-005.4 (含接著劑/填縫劑的複合Set導入) needed a way to keep adhesive/sealant/waterproofing materials out of the CompoundStructure layer list entirely. Frontend: added a second drop zone (`auxCompositionList`) to the layer-composition modal, separate from the existing Structure/Substrate/Finish layer list (`layerCompositionList`); rewrote the single-list drag state (`layerDragFromIdx`) into a zone-aware one (`layerDragFrom: {zone, idx}`) so rows can be dragged bidirectionally between the two zones via `moveDraggedEntry` (Core Boundary markers are blocked from entering the aux zone); added `classifyAuxiliaryKeyword(title)` mirroring the Python engine's keyword rules exactly, used by `buildDefaultLayerDraft()` to auto-route new Set materials into the aux zone on open rather than defaulting them into a geometric role; `layerComposition` gained an `auxiliary: [{licno, auxType}]` field alongside the existing `sequence`. Backend: `generate_revit_injection_plan.py` gained `_build_layer_composition_aux_map`/`_resolve_layer_composition_auxiliary` so the plan engine honors the UI's explicit aux classification (overriding keyword guessing, same authority level as the existing Structure/Substrate/Finish override). While wiring this up, found and fixed a related latent bug: `_assign_material_slots` was letting auxiliary materials compete for Mat1~Mat6 shared-parameter slots (they landed in the lowest-priority 'Other' bucket via `_infer_role_bucket` but still consumed a slot ahead of running out) even though they don't have a CompoundStructure layer at all and get their own dedicated `GreenMaterial_Adhesive/Sealant/Waterproofing` field — excluded them from slot competition entirely, added an `"auxiliary"` sentinel for their `assignedSlot`, and fixed the Markdown report's overflow-section trigger to count only slot-eligible materials. Verified via `node --check` (full inline-script extraction), a standalone Node unit test of the classification/routing logic (exact screenshot scenario: GBM0104110 填縫劑 correctly separates from GBM0102995 陶瓷面磚, including an all-auxiliary edge case), and a Python test against `generate_injection_plan` with a mocked Set (real DB records, `load_exported_sets` monkeypatched to avoid touching the real Set store) confirming `isAuxiliary`/`GreenMaterial_Sealant`/`assignedSlot="auxiliary"` and correct exclusion from `materialSlotAssignment`. The Python test runs briefly overwrote the real `Revit_Injection_Plan.json`/report with test data (not gitignored) — restored by re-running `generate_injection_plan` for the real pending Set (地磚test) immediately after. `verify-qaqc.ps1 -SkipBuild -SkipDeploy`: 54 PASS / 0 FAIL. Kanban card moved ready -> implementing, ui gate true; still missing the actual Revit-write half (no MCP tool yet writes `GreenMaterial_Adhesive/Sealant/Waterproofing` to a Parent Type's Construction fields, and only a Floor case has been exercised, not Wall) — noted explicitly on the card as the next step, not attempted this round.

## [2026-08-11 02:03] fix+feat | TASK-005.4 — corrected Mat-slot exclusion per user, added the actual Revit-write path for auxiliary materials
- actor: claude-sonnet-5 (via Claude Code)
- files: generate_revit_injection_plan.py, GreenMaterial_SharedParams.txt, MCP/Core/Commands/CommandExecutor.GreenMaterial.cs, MCP-Server/src/tools/visualization-tools.ts, .claude/skills/import/SKILL.md, tools/kanban/cards/TASK-005.4.json, kanban.html, tools/kanban/index.html
- trigger: manual (user, after seeing the `/GMimport` plan for Set 【地磚與填縫】: "輔助類材料會確實進入Mat1~Mat6的欄位，Mat這個欄位的目的是看該元件包含哪些綠建材用的，修正後開始開發導入REVIT的工具" — also gave a standing instruction: only ask for confirmation on important actions, let everything else proceed automatically)
- summary: Correction first — the previous session's change to `_assign_material_slots` (excluding `isAuxiliary` materials from Mat1~Mat6 competition) was based on a wrong assumption. The user clarified Mat1~Mat6 is a manifest of every green material the component contains, not exclusively a physical-layer registry, so auxiliary materials should compete for slots normally. Reverted `_assign_material_slots`, the Markdown report's slot-line rendering, and the "2b" overflow-section trigger back to their pre-"fix" form (verified: GBM0104110 填縫劑 now correctly lands on `mat2` in the real Set 【地磚與填縫】, re-synced via `write_back_to_set_manager`). Then built the actual missing piece: the Revit-write path for `GreenMaterial_Adhesive/Sealant/Waterproofing`, which never existed anywhere (not in the shared-param file, not in C#, not in the TS tool schema — the Python plan engine's `sharedParameters[auxiliaryParam]` was pure planning metadata with nowhere to land). Added a new GROUP 4 "非幾何輔助材料 (Construction)" and 3 TEXT params to `GreenMaterial_SharedParams.txt` (GUIDs ...165-167, continuing the existing sequential scheme; edited via a Python script operating in cp950 per the file's own encoding warning, verified with `validate_shared_params.py` — 67 params, 4 groups, no duplicate GUIDs, and a UTF-8 round-trip check confirmed the Chinese text wasn't corrupted). Extended `SetGreenMaterialTypeParameters` (C#) with three new top-level string params (`adhesive`/`sealant`/`waterproofing`) written via the existing `SetTypeParamText` helper, and the matching TS tool schema in `visualization-tools.ts`. Updated `/import` skill's Scenario 3: step 3 now explicitly skips `isAuxiliary` items when building the physical `layers` array (they have no `layerFunction`/thickness) while step 9 clarifies they still need a normal `matN` slot *plus* the corresponding top-level aux param; step 7 notes auxiliary materials won't show up in `get_all_materials` by design (no Revit `Material` element is ever created for them — only Identity Data text). Verified via `dotnet build -c Release.R24` (0 errors) and `npm run build` (MCP-Server, clean); not yet deployed or tested against a live Revit connection — deploying would require closing/reopening Revit again (the session already hit two accidental data-loss cycles today from closing Revit without saving), so held off pending explicit user go-ahead per their "confirm important actions" instruction. Kanban card content corrected and expanded, architecture gate now true, test gate still false pending the live run.

## [2026-08-11 02:14] feat | TASK-005.4 end-to-end Revit run — auxiliary-material Construction field confirmed working (Set 地磚與填縫)
- actor: claude-sonnet-5 (via Claude Code)
- files: exported_material_sets.json, Revit_Injection_Plan.json (generated), tools/kanban/cards/TASK-005.4.json, kanban.html, tools/kanban/index.html
- trigger: manual (user closed and reopened Revit specifically to pick up the newly deployed DLL, then said so)
- summary: Deployed the DLL built after the `GreenMaterial_Adhesive/Sealant/Waterproofing` work (Revit was already closed when asked, so no confirmation round-trip needed this time) and confirmed live via `get_project_info` after the user reopened Revit. Ran Scenario 3 for the real pending Set 【地磚與填縫】 (GBM0104110 CG2WA泰固美特耐磁磚填縫劑 as Sealant + GBM0102995 陶瓷面磚 as the sole Structure layer, per the Set's saved `layerComposition`). `load_shared_parameters` confirmed the 3 new Construction-group params bind successfully to Floors (previously nonexistent, now live). Source Type 339 (通用 - 15cm); asked the user for the tile's actual thickness since the plan's generic 150mm Structure default was clearly wrong for a tile layer — user picked 10mm. `create_multi_layer_type` built `TABC_地磚與填縫` (Element ID 264332, Material ID 264334) with exactly 1 physical layer, confirming the auxiliary material was correctly excluded from CompoundStructure. `set_green_material_type_parameters` wrote mat1 (tile) + mat2 (sealant, now competing normally per the reverted slot logic) + the new `sealant` top-level param in one call — 24/24 written, 0 missing. `get_element_info` confirmed `GreenMaterial_Sealant = "CG2WA泰固美特耐磁磚填縫劑 (GBM0104110)"` alongside both Mat1/Mat2 blocks. `exported_material_sets.json` updated to reflect completion. This is the first live confirmation that the whole TASK-005.4 pipeline (UI drop zone → plan engine → new shared params → C#/TS tool → `/import` skill) works end-to-end on a real user-built Set, not just a mocked one. Kanban card moved implementing -> verify, test gate now true; still only Floor has been exercised, Wall remains unverified.

## [2026-08-11 02:21] chore | Deleted TASK-005.9 (隔熱/防音底層 Set 導入)
- actor: claude-sonnet-5 (via Claude Code)
- files: tools/kanban/cards/TASK-005.9.json (deleted), tools/kanban/cards/TASK-011.json, tools/kanban/cards/TASK-008.json, kanban.html, tools/kanban/index.html
- trigger: manual (user: "可以刪除TASK005.9了")
- summary: Unlike the earlier reorg where overlapping cards were archived (`done` + merge note) rather than deleted, the user explicitly asked to delete this one outright, so removed `tools/kanban/cards/TASK-005.9.json`. Rationale carried over from the earlier merge note (TASK-011): putting a material in the Substrate/Insulation `layerFunction` is just one option `create_multi_layer_type` already handles generically, same as Structure/Finish — there was no distinct capability left for a dedicated "情境9" card to track once TASK-011's tool absorbed it. Cleaned up the two remaining references: TASK-011's merge note (dropped the "／TASK-005.9" mentions, kept TASK-005.10's), and TASK-008's progress list (removed the 情境9 line, corrected "11 情境/11 張子卡" wording to reflect 10 remaining scenario cards, and folded in TASK-005.4's now-verified status while at it). No other card had `TASK-005.9` in a `dependsOn` array. Regenerated both kanban HTML embeds (24 cards, confirmed byte-identical and `TASK-005.9` absent via `node -e` eval). `verify-qaqc.ps1 -SkipBuild -SkipDeploy`: 54 PASS / 0 FAIL.
