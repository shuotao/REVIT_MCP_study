# Event Log — 2026-08

> Append-only project development log.

## [2026-08-03 19:31] kanban | 將 TASK-005.x 重整為 11 種 Set 導入 Revit 情境卡
- actor: GPT-5 (via Codex)
- files: tools/kanban/cards/TASK-005.1.json ~ TASK-005.11.json, tools/kanban/cards/TASK-008.json, tools/kanban/cards/TASK-009.json, kanban.html, tools/kanban/index.html
- trigger: manual
- summary: 依 TASK-003 Master Mapping 將 TASK-005 子卡改為情境 1～11；情境 1、10 設為 verify，其餘設為 ready，並同步上層驗證卡及兩份看板頁面。

## [2026-08-04 02:28] archive | organize green-material Revit development assets
- actor: GPT-5 (via Codex)
- files: tools/green-material/**, docs/green-material/**, generate_revit_injection_plan.py, .claude/skills/GMimport/SKILL.md, README.md, README.zh-TW.md, docs/DOCUMENT_AUDIENCE_INVENTORY.md
- trigger: manual
- summary: Classified historical green-material scripts, intermediate data, reports, and snapshots while preserving established runtime entry points and MCP source locations.

## [2026-08-06 17:30] feat | Set manager UI additions, layerComposition-aware plan engine, v5 six-slot shared-parameter schema
- actor: claude-sonnet-5 (via Claude Code)
- files: assets/green-material-showcase.html, .claude/skills/GMweb/SKILL.md, generate_revit_injection_plan.py, GreenMaterial_SharedParams.txt, MCP/Core/Commands/CommandExecutor.GreenMaterial.cs, MCP-Server/src/tools/visualization-tools.ts, MCP-Server/src/tools/base-tools.ts, domain/green-material-parameter-schema.md, .claude/skills/import/SKILL.md, tools/kanban/cards/TASK-013.json, tools/kanban/cards/TASK-014.json, tools/kanban/cards/TASK-015.json, kanban.html, tools/kanban/index.html
- trigger: manual
- summary: Added "add to existing Set", project-file export/import, and a draggable material-layer (Structure/Substrate/Finish/Core Boundary) UI to the showcase page plus `/GMweb open`; made the injection-plan engine honor `layerComposition` and assign Mat slots deterministically (`_assign_material_slots`); expanded GreenMaterial_SharedParams.txt from v4 (Mat1-3, 31 fields) to v5 (Mat1-6, 64 fields) across C#/MCP-tool/Python, rebuilt and redeployed the Revit 2024 add-in, verified end-to-end in a test document, and fixed a Mat-slot-order-vs-physical-layer-order mixup in the `/import revit` skill instructions. Kanban regenerated from `tools/kanban/cards/*.json` (also picked up 7 previously-unsynced TASK-005.x cards).

## [2026-08-07 10:15] feat | Add /GMupdate and /GMset compare skills for refreshing and diffing TABC source data
- actor: claude-sonnet-5 (via Claude Code)
- files: update_tabc_database.py, generate_revit_injection_plan.py, .claude/skills/GMupdate/SKILL.md, .claude/skills/GMset/SKILL.md, tools/green-material/README.md, CLAUDE.md, README.md, README.zh-TW.md, docs/DOCUMENT_AUDIENCE_INVENTORY.md, docs/BIM_MCP/index.html, docs/BIM_MCP/reference/skills-index.html
- trigger: manual
- summary: There was previously no way to refresh `tabc_master_database.json` from the live TABC site or to check whether an existing material Set had drifted from current source data. Added `update_tabc_database.py` (GBMTYPE 1-4 list-page crawl, merge-not-overwrite so a partial crawl never wipes real records, re-syncs `assets/green-material-showcase.html`'s embedded offline cache) driven by the new `/GMupdate` skill, and `compare_all_sets()`/`compare_and_refresh_set()` in `generate_revit_injection_plan.py` (diffs a Set's licnos against the current master DB for missing/expired/renamed materials, using the licno→title snapshot embedded in each Set's `purpose` text since `Revit_Injection_Plan.json` itself is overwritten on every run) driven by the new `/GMset compare` skill — refresh only touches plan/report files, never Revit directly. Verified both live against the real TABC site (dry-run crawl succeeded, compare ran cleanly against the current Set). Bumped the Claude skills count 54 -> 56 across CLAUDE.md/README.md/README.zh-TW.md/docs/DOCUMENT_AUDIENCE_INVENTORY.md/docs/BIM_MCP site pages per the count-table gate.

## [2026-08-07 10:45] fix | Green-material keyword mention no longer auto-opens the showcase webpage
- actor: claude-sonnet-5 (via Claude Code)
- files: domain/green-material-catalog.md, domain/green-material-keyword-search.md
- trigger: manual
- summary: `domain/green-material-catalog.md` (§5, §5.1, §6) and `domain/green-material-keyword-search.md` (§0, §2) previously instructed the AI to automatically pop open `assets/green-material-showcase.html` the moment a green-material keyword was mentioned in conversation. Changed both to ask the user first and only run `/GMweb open` after confirmation (skip the ask only when the user's message is already an explicit open request). Also fixed §5.1/§6 in `green-material-catalog.md`, which still described writing per-search-result JSON directly into the showcase HTML's dynamic library — that's stale versus the actual `/GMupdate` (`update_tabc_database.py`) merge-based refresh mechanism added earlier today; both now point at `/GMupdate` instead. The equivalent policy was also added to the ported `green-material-search-toolkit` skill in the separate HJPLUS_Taiwan_Architect_KB clone (its own `SKILL.md`, new "Trigger Confirmation Rule" section) so both projects behave the same way.

## [2026-08-07 11:05] fix | Remove duplicate /GM search branch (redundant with /GMweb open)
- actor: claude-sonnet-5 (via Claude Code)
- files: .claude/skills/GM/SKILL.md, .claude/skills/GMweb/SKILL.md, .claude/skills/GMset/SKILL.md, .claude/skills/GMupdate/SKILL.md, docs/BIM_MCP/reference/skills-index.html
- trigger: manual
- summary: `/GM search` and `/GMweb open` opened the exact same page via identical steps (check port 8888, start local_server.py if needed, open browser) — a pointless duplication. Narrowed `/GM` to kanban-only (its `search` action and all port/server logic removed); `/GMweb open` is now the single entry point for the green-material search & Set Manager page. Updated cross-references in GMset/GMupdate SKILL.md and the skills-index.html doc page accordingly. Skill count unchanged (56) — no file was deleted, `/GM` was narrowed in place.

## [2026-08-11 00:46] feat | TASK-005.2 — set_material_surface_pattern MCP tool for Floor tile/wood Surface Pattern
- actor: claude-sonnet-5 (via Claude Code)
- files: MCP/Core/Commands/CommandExecutor.GreenMaterial.cs, MCP/Core/CommandExecutor.cs, MCP-Server/src/tools/visualization-tools.ts, .claude/skills/import/SKILL.md, tools/kanban/cards/TASK-005.2.json, kanban.html, tools/kanban/index.html, CLAUDE.md, README.md, README.zh-TW.md, docs/DOCUMENT_AUDIENCE_INVENTORY.md, docs/BIM_MCP/reference/skills-index.html, docs/BIM_MCP/reference/personal-llm-wiki.html, docs/BIM_MCP/reference/philosophy-22-propositions.html, docs/BIM_MCP/reference/domain-index.html, docs/BIM_MCP/reference/architecture-v2.html
- trigger: manual
- summary: Started TASK-005.2 (地磚與表面填充 Set 導入). Floor Type duplication, Substrate/Finish1 thickness defaults (15mm/20mm), and shared-parameter binding were already covered generically by TASK-005.1's `create_multi_layer_type`/`create_single_material_type`/`load_shared_parameters` tools — the one genuinely missing capability was the acceptance criteria's Surface Pattern requirement (600×600 grid for tile, wood-grain for flooring). Added `set_material_surface_pattern` (new C# method + dispatcher case + TS tool definition): builds or reuses (dedup by FillPatternElement name) a Model-target `FillPattern` — a 0°/90° grid for tile joints or a single-direction offset line pattern for wood grain — and assigns it to a Material's `SurfaceForegroundPatternId`/`CutForegroundPatternId`. Wired it into `/import` skill Scenario 2 (step 5b) and Scenario 3 (step 6b) so Floor finish-layer materials get a pattern applied right after Type creation. Verified via `dotnet build -c Release.R24` (0 errors) and `npm run build` (MCP-Server, clean). Runtime tool count 175 -> 176, synced across CLAUDE.md/README/README.zh-TW/audience inventory/BIM_MCP reference pages. Kanban card moved ready -> implementing (architecture gate now true); test/code_review gates remain open pending an actual Revit end-to-end run with a real tile Set, which needs a live Revit connection and user confirmation per the skill's mutation-confirm rule — not run in this session.

## [2026-08-11 01:00] feat | TASK-005.2 end-to-end Revit run via /GMimport -> /import revit (Set 地磚test)
- actor: claude-sonnet-5 (via Claude Code)
- files: exported_material_sets.json, Revit_Injection_Plan.json (generated, not tracked), tools/kanban/cards/TASK-005.2.json (content only, superseded by the 01:23 reorg entry below)
- trigger: manual
- summary: Ran `/GMimport` (Set 【地磚test】, GBM0104033 透水性混凝土地磚, Floor/單一組合, 1 material — no separate substrate licno) then `/import revit`. Deployed the new `set_material_surface_pattern` DLL to `%APPDATA%\Autodesk\Revit\Addins\2024\RevitMCP\` (Revit was confirmed running 2024) and confirmed via a live `get_project_info` re-anchor each step. Hit two full data-loss cycles where the Revit document was closed without saving between steps (verified via `get_all_materials`/`get_types_by_category` returning empty each time) — rebuilt the Floor Type from scratch both times before the user confirmed a real save. Final successful run: `create_multi_layer_type` built `TABC_地磚test` (Element ID 264193, source TypeId 339) with Finish1 GBM0104033_透水性混凝土地磚 (Material ID 264195, 15mm, user-confirmed via AskUserQuestion) + Substrate 1-3水泥砂漿打底 (Material ID 264197, non-green generic material, 20mm, added after asking the user since the Set had no licno for the mortar layer); `set_material_surface_pattern` applied `TABC_Grid_600x600` (Pattern ID 264214) to the tile material's Surface pattern — first live use of the new tool; 13/13 shared parameters written with zero `MissingParameters`, verified via `get_element_info`; `exported_material_sets.json` updated to `已完成 Revit 牆體元件注入`. TASK-005.2 moved to `verify` stage (see 01:23 kanban reorg entry for the card content update).

## [2026-08-11 01:23] chore | Kanban reorganization — merge overlapping infra cards, fix broken dependency, re-derive order sequence
- actor: claude-sonnet-5 (via Claude Code)
- files: tools/kanban/cards/TASK-005.2.json, TASK-005.9.json, TASK-005.10.json, TASK-009.json, TASK-010.json, TASK-011.json, TASK-012.json, TASK-007.json, TASK-008.json, TASK-001/002/003/004/005.1/005.3-005.8/005.11/006/013/014/015.json (order field only), tools/kanban/index.html, kanban.html
- trigger: manual (user: "看板有點亂...重工的任務...請幫我重新整理一遍，並重新排序")
- summary: User flagged the kanban as cluttered with duplicate work. Root cause: two independent "情境/scenario" numbering schemes collided — the original 11-scenario planning cards (`TASK-005.1`~`TASK-005.11`, created 08/03) and the `/import` skill's internal Scenario 1/2/3 path names, which later became their own dev cards (`TASK-009`/`010`/`011`, built 08/03-08/04) describing the *same* underlying tool work (duplicate_element_type for TASK-005.1, create_single_material_type for TASK-005.10's Floors path, create_multi_layer_type for TASK-005.9's soundproof case and TASK-005.10's Floors multi-material path). Also found a broken `dependsOn: ["TASK-005"]` on TASK-007 (no such card ID exists, only 005.1-005.11 sub-cards) and TASK-008's progress notes stale relative to actual tool state. Per user's confirmed "完整整併" (full merge) choice: marked TASK-009/010/011 `done` with in-content merge notes pointing to their target 005.x card (kept, not deleted, for git-traceable history — TASK-010 explicitly noted as cross-cutting infra not owned by any single 005.x card, to avoid overclaiming); ticked TASK-005.10's Mat3/Floors-multi-material/traceability criteria with evidence citations (Ceilings case and final review still open); added a partial-evidence note to TASK-005.9 without falsely ticking its Wall/insulation-still-missing criteria; marked TASK-012 (resolved "forgot to save" incident) `done` with a note that future incident write-ups belong in `log/` rather than a kanban card; fixed TASK-007's dependency to point at TASK-005.1/TASK-005.2 instead of the dangling ID (avoided a 007<->008 cycle); updated TASK-008's scenario-by-scenario progress list to reflect 005.1/005.2 now complete and 005.9/005.10 partially evidenced. Re-derived every card's `order` field into a single Epic-grouped sequence (Epic-1: 1-3, Epic-2: 4-8.97 with 005.x sub-scenarios at 8.1-8.96 and merged infra cards slotted at collision-safe decimals like 8.11/8.91/8.955, Epic-3: 10, Epic-4: 11-12), replacing the old scheme that interleaved Epic-1/3/4 whole-number orders (6,7,8,13) in between the Epic-2 005.x decimal block. Regenerated the `cardsData` embed in both `kanban.html` and `tools/kanban/index.html` from the 25 updated card files via a one-off Python script (CRLF-preserving), verified both embeds are byte-identical and still valid JS (`node -e` eval parse, 25 unique IDs). No cards deleted.

## [2026-08-11 01:48] feat | TASK-005.4 — auxiliary-material drop zone in the layer-composition modal + plan-engine wiring
- actor: claude-sonnet-5 (via Claude Code)
- files: assets/green-material-showcase.html, generate_revit_injection_plan.py, tools/kanban/cards/TASK-005.4.json, kanban.html, tools/kanban/index.html
- trigger: manual (user showed a screenshot: the 材料層級設定 modal was forcing GBM0104110, a sealant/填縫劑, into the "Structure（結構層）" dropdown because the modal only offered Structure/Substrate/Finish — no place for non-geometric auxiliary materials)
- summary: TASK-005.4 (含接著劑/填縫劑的複合Set導入) needed a way to keep adhesive/sealant/waterproofing materials out of the CompoundStructure layer list entirely. Frontend: added a second drop zone (`auxCompositionList`) to the layer-composition modal, separate from the existing Structure/Substrate/Finish layer list (`layerCompositionList`); rewrote the single-list drag state (`layerDragFromIdx`) into a zone-aware one (`layerDragFrom: {zone, idx}`) so rows can be dragged bidirectionally between the two zones via `moveDraggedEntry` (Core Boundary markers are blocked from entering the aux zone); added `classifyAuxiliaryKeyword(title)` mirroring the Python engine's keyword rules exactly, used by `buildDefaultLayerDraft()` to auto-route new Set materials into the aux zone on open rather than defaulting them into a geometric role; `layerComposition` gained an `auxiliary: [{licno, auxType}]` field alongside the existing `sequence`. Backend: `generate_revit_injection_plan.py` gained `_build_layer_composition_aux_map`/`_resolve_layer_composition_auxiliary` so the plan engine honors the UI's explicit aux classification (overriding keyword guessing, same authority level as the existing Structure/Substrate/Finish override). While wiring this up, found and fixed a related latent bug: `_assign_material_slots` was letting auxiliary materials compete for Mat1~Mat6 shared-parameter slots (they landed in the lowest-priority 'Other' bucket via `_infer_role_bucket` but still consumed a slot ahead of running out) even though they don't have a CompoundStructure layer at all and get their own dedicated `GreenMaterial_Adhesive/Sealant/Waterproofing` field — excluded them from slot competition entirely, added an `"auxiliary"` sentinel for their `assignedSlot`, and fixed the Markdown report's overflow-section trigger to count only slot-eligible materials. Verified via `node --check` (full inline-script extraction), a standalone Node unit test of the classification/routing logic (exact screenshot scenario: GBM0104110 填縫劑 correctly separates from GBM0102995 陶瓷面磚, including an all-auxiliary edge case), and a Python test against `generate_injection_plan` with a mocked Set (real DB records, `load_exported_sets` monkeypatched to avoid touching the real Set store) confirming `isAuxiliary`/`GreenMaterial_Sealant`/`assignedSlot="auxiliary"` and correct exclusion from `materialSlotAssignment`. The Python test runs briefly overwrote the real `Revit_Injection_Plan.json`/report with test data (not gitignored) — restored by re-running `generate_injection_plan` for the real pending Set (地磚test) immediately after. `verify-qaqc.ps1 -SkipBuild -SkipDeploy`: 54 PASS / 0 FAIL. Kanban card moved ready -> implementing, ui gate true; still missing the actual Revit-write half (no MCP tool yet writes `GreenMaterial_Adhesive/Sealant/Waterproofing` to a Parent Type's Construction fields, and only a Floor case has been exercised, not Wall) — noted explicitly on the card as the next step, not attempted this round.

## [2026-08-11 02:03] fix+feat | TASK-005.4 — corrected Mat-slot exclusion per user, added the actual Revit-write path for auxiliary materials
- actor: claude-sonnet-5 (via Claude Code)
- files: generate_revit_injection_plan.py, GreenMaterial_SharedParams.txt, MCP/Core/Commands/CommandExecutor.GreenMaterial.cs, MCP-Server/src/tools/visualization-tools.ts, .claude/skills/import/SKILL.md, tools/kanban/cards/TASK-005.4.json, kanban.html, tools/kanban/index.html
- trigger: manual (user, after seeing the `/GMimport` plan for Set 【地磚與填縫】: "輔助類材料會確實進入Mat1~Mat6的欄位，Mat這個欄位的目的是看該元件包含哪些綠建材用的，修正後開始開發導入REVIT的工具" — also gave a standing instruction: only ask for confirmation on important actions, let everything else proceed automatically)
- summary: Correction first — the previous session's change to `_assign_material_slots` (excluding `isAuxiliary` materials from Mat1~Mat6 competition) was based on a wrong assumption. The user clarified Mat1~Mat6 is a manifest of every green material the component contains, not exclusively a physical-layer registry, so auxiliary materials should compete for slots normally. Reverted `_assign_material_slots`, the Markdown report's slot-line rendering, and the "2b" overflow-section trigger back to their pre-"fix" form (verified: GBM0104110 填縫劑 now correctly lands on `mat2` in the real Set 【地磚與填縫】, re-synced via `write_back_to_set_manager`). Then built the actual missing piece: the Revit-write path for `GreenMaterial_Adhesive/Sealant/Waterproofing`, which never existed anywhere (not in the shared-param file, not in C#, not in the TS tool schema — the Python plan engine's `sharedParameters[auxiliaryParam]` was pure planning metadata with nowhere to land). Added a new GROUP 4 "非幾何輔助材料 (Construction)" and 3 TEXT params to `GreenMaterial_SharedParams.txt` (GUIDs ...165-167, continuing the existing sequential scheme; edited via a Python script operating in cp950 per the file's own encoding warning, verified with `validate_shared_params.py` — 67 params, 4 groups, no duplicate GUIDs, and a UTF-8 round-trip check confirmed the Chinese text wasn't corrupted). Extended `SetGreenMaterialTypeParameters` (C#) with three new top-level string params (`adhesive`/`sealant`/`waterproofing`) written via the existing `SetTypeParamText` helper, and the matching TS tool schema in `visualization-tools.ts`. Updated `/import` skill's Scenario 3: step 3 now explicitly skips `isAuxiliary` items when building the physical `layers` array (they have no `layerFunction`/thickness) while step 9 clarifies they still need a normal `matN` slot *plus* the corresponding top-level aux param; step 7 notes auxiliary materials won't show up in `get_all_materials` by design (no Revit `Material` element is ever created for them — only Identity Data text). Verified via `dotnet build -c Release.R24` (0 errors) and `npm run build` (MCP-Server, clean); not yet deployed or tested against a live Revit connection — deploying would require closing/reopening Revit again (the session already hit two accidental data-loss cycles today from closing Revit without saving), so held off pending explicit user go-ahead per their "confirm important actions" instruction. Kanban card content corrected and expanded, architecture gate now true, test gate still false pending the live run.

## [2026-08-11 02:14] feat | TASK-005.4 end-to-end Revit run — auxiliary-material Construction field confirmed working (Set 地磚與填縫)
- actor: claude-sonnet-5 (via Claude Code)
- files: exported_material_sets.json, Revit_Injection_Plan.json (generated), tools/kanban/cards/TASK-005.4.json, kanban.html, tools/kanban/index.html
- trigger: manual (user closed and reopened Revit specifically to pick up the newly deployed DLL, then said so)
- summary: Deployed the DLL built after the `GreenMaterial_Adhesive/Sealant/Waterproofing` work (Revit was already closed when asked, so no confirmation round-trip needed this time) and confirmed live via `get_project_info` after the user reopened Revit. Ran Scenario 3 for the real pending Set 【地磚與填縫】 (GBM0104110 CG2WA泰固美特耐磁磚填縫劑 as Sealant + GBM0102995 陶瓷面磚 as the sole Structure layer, per the Set's saved `layerComposition`). `load_shared_parameters` confirmed the 3 new Construction-group params bind successfully to Floors (previously nonexistent, now live). Source Type 339 (通用 - 15cm); asked the user for the tile's actual thickness since the plan's generic 150mm Structure default was clearly wrong for a tile layer — user picked 10mm. `create_multi_layer_type` built `TABC_地磚與填縫` (Element ID 264332, Material ID 264334) with exactly 1 physical layer, confirming the auxiliary material was correctly excluded from CompoundStructure. `set_green_material_type_parameters` wrote mat1 (tile) + mat2 (sealant, now competing normally per the reverted slot logic) + the new `sealant` top-level param in one call — 24/24 written, 0 missing. `get_element_info` confirmed `GreenMaterial_Sealant = "CG2WA泰固美特耐磁磚填縫劑 (GBM0104110)"` alongside both Mat1/Mat2 blocks. `exported_material_sets.json` updated to reflect completion. This is the first live confirmation that the whole TASK-005.4 pipeline (UI drop zone → plan engine → new shared params → C#/TS tool → `/import` skill) works end-to-end on a real user-built Set, not just a mocked one. Kanban card moved implementing -> verify, test gate now true; still only Floor has been exercised, Wall remains unverified.

## [2026-08-11 02:21] chore | Deleted TASK-005.9 (隔熱/防音底層 Set 導入)
- actor: claude-sonnet-5 (via Claude Code)
- files: tools/kanban/cards/TASK-005.9.json (deleted), tools/kanban/cards/TASK-011.json, tools/kanban/cards/TASK-008.json, kanban.html, tools/kanban/index.html
- trigger: manual (user: "可以刪除TASK005.9了")
- summary: Unlike the earlier reorg where overlapping cards were archived (`done` + merge note) rather than deleted, the user explicitly asked to delete this one outright, so removed `tools/kanban/cards/TASK-005.9.json`. Rationale carried over from the earlier merge note (TASK-011): putting a material in the Substrate/Insulation `layerFunction` is just one option `create_multi_layer_type` already handles generically, same as Structure/Finish — there was no distinct capability left for a dedicated "情境9" card to track once TASK-011's tool absorbed it. Cleaned up the two remaining references: TASK-011's merge note (dropped the "／TASK-005.9" mentions, kept TASK-005.10's), and TASK-008's progress list (removed the 情境9 line, corrected "11 情境/11 張子卡" wording to reflect 10 remaining scenario cards, and folded in TASK-005.4's now-verified status while at it). No other card had `TASK-005.9` in a `dependsOn` array. Regenerated both kanban HTML embeds (24 cards, confirmed byte-identical and `TASK-005.9` absent via `node -e` eval). `verify-qaqc.ps1 -SkipBuild -SkipDeploy`: 54 PASS / 0 FAIL.

## [2026-08-12 02:30] feat | TASK-005.7 — RFA family (Window/Door) green-material injection: domain SOP + Revit tool chain built and build-verified
- actor: claude-sonnet-5 (via Claude Code)
- files: domain/rfa-family-injection.md (new), CLAUDE.md, README.md, README.zh-TW.md, docs/DOCUMENT_AUDIENCE_INVENTORY.md, docs/BIM_MCP/reference/{architecture-v2,personal-llm-wiki,philosophy-22-propositions,skills-index,three-constitutions,contributor-template,domain-index,industry-evidence}.html, GreenMaterial_SharedParams.txt, MCP/Core/Commands/CommandExecutor.RfaFamilyInjection.cs (new), MCP/Core/Commands/CommandExecutor.GreenMaterial.cs, MCP/Core/CommandExecutor.cs, MCP-Server/src/tools/visualization-tools.ts, MCP-Server/src/tools/revit-tools.ts, .claude/skills/import/SKILL.md, tools/kanban/cards/TASK-005.7.json
- trigger: manual (user: "請問要如何進行", then confirmed via AskUserQuestion to author the domain SOP first and use dedicated shared params for shading/acoustic fields, then "請繼續")
- summary: TASK-005.7 (情境7, 門窗/獨立元件 RFA 綠建材導入) previously had zero Revit-side implementation — the `/import` skill explicitly told users to stop on this path. Unlike every other TASK-005.x scenario, this one operates on a separate loadable-family (.rfa) document rather than the live project document, so it needed a new domain SOP first (`domain/rfa-family-injection.md`, 4 hard rules: no-generation-from-scratch, backup-before-any-edit, Identity Data + dedicated shading/acoustic param placement, new-family-name to dodge LoadFamily overwrite ambiguity). Added `GreenMaterial_Window_ShadingCoefficient`/`GreenMaterial_AcousticRw` (new GROUP 5) to `GreenMaterial_SharedParams.txt` — edited via a cp950-safe Python script per the file's own encoding warning (one accidental truncation caught and restored via `git restore` immediately, then redone correctly with full in-memory encode before any file write). Built `inject_green_material_into_family` (new C# file + TS tool + dispatcher case): a single orchestrating call covering EditFamily -> SaveAs backup -> new Type in the family doc -> write Identity Data + GreenMaterial_Mat1_*/shading/acoustic params -> SaveAs under a new family filename -> LoadFamily back into the project, with a same-transaction pre/post Type-signature snapshot diff that rolls back automatically if any non-target sibling Type would be affected. While build-verifying, discovered via `System.Reflection.MetadataLoadContext` reflection against the actual `Nice3point.Revit.Api.RevitApi` reference assemblies (2022-2026) that `FamilyManager.NewFamilyParameter` never existed under that name in any of these versions (the real API is `AddParameter`) and that `BuiltInParameterGroup` was removed entirely from the 2025/2026 API — the latter was already silently breaking `CommandExecutor.GreenMaterial.cs`'s `LoadSharedParameters` fallback for any R25/R26 build, a pre-existing bug unrelated to this task, fixed in passing (`#if !REVIT2024_OR_GREATER` guard) since it blocked verifying this change on modern Revit versions at all. `dotnet build` clean (0 errors) across all 5 configs (R22-R26); MCP-Server `tsc --noEmit` and `npm run build` clean; `registerRevitTools()` confirmed 178 unique tools including the new one. Tool count 177->178 and domain count 75->76 synced across every source-of-truth file per CLAUDE.md's count-table gate. Wired Scenario 7 into `/import` SKILL.md (not Set-driven — user names the base FamilySymbol directly, no `exported_material_sets.json` convention exists for Window/Door yet). `verify-qaqc.ps1 -SkipBuild -SkipDeploy`: 54 PASS / 0 FAIL. Kanban card moved ready -> implementing. Not yet deployed or tested against a live Revit connection — the acceptance criteria's "at least one Window and one Door case" live validation still needs an actual Revit session and explicit user go-ahead before any DLL deploy/Revit restart, not attempted this session.

## [2026-08-12 02:50] feat | TASK-005.7 end-to-end Revit run — Window + Door RFA injection confirmed working, kanban moved to verify
- actor: claude-sonnet-5 (via Claude Code)
- files: tools/kanban/cards/TASK-005.7.json, kanban.html, tools/kanban/index.html
- trigger: manual (user closed Revit for the DLL deploy, reopened it, then reloaded the MCP-Server connection when the new tool wasn't showing up yet)
- summary: Deployed `MCP/bin/Release.R24/RevitMCP.dll` to `%APPDATA%\Autodesk\Revit\Addins\2024\RevitMCP\` after the user confirmed Revit was closed. After the user reopened Revit, `inject_green_material_into_family` was still missing from the live MCP tool list — traced to the MCP-Server (Node) process itself having been started before this session's `npm run build`, so its in-memory tool registry was stale independent of the Revit-side DLL; the user reloaded the MCP connection and the tool appeared. Per domain rule 1, asked the user to pick real base Types rather than guessing: `list_family_symbols`/`query_elements_with_filter` surfaced existing candidates in the open project ("綠建材導入Revet_模型範例1_vitolin50310"), user picked 雙開落地窗-(2) 180x210cm (ID 168921) for Window and 單開-矩形-(1) 90x210cm (Type Id 166496, from a placed instance) for Door. Sourced real green-material records from `tabc_master_database.json` instead of inventing data: GBM0104092 氣密隔音固定窗 (Rw 42dB, no shading-coefficient data in the record) for Window, GBM0103215 賜福隔音門 (Rw 42dB) for Door — confirmed with the user via AskUserQuestion before writing. Both `inject_green_material_into_family` calls succeeded: Window -> new Type 180 x 210 cm_TABC_GBM0104092 (Element ID 267643, family file `雙開落地窗- (2)_TABC_GBM0104092.rfa`), Door -> new Type 90 x 210 cm_TABC_GBM0103215 (Element ID 269404, family file `單開-矩形- (1)_TABC_GBM0103215.rfa`); both reported `AffectedExistingTypes: 0` and zero `MissingParameters` (Window correctly omitted `GreenMaterial_Window_ShadingCoefficient` since no real data existed, not treated as a failure). Verified via `get_element_info` on both new Types (all `GreenMaterial_Mat1_*`/`GreenMaterial_AcousticRw` values match) and re-queried the original door instance (255845, Type Id 166496) to confirm it was untouched. All 5 acceptance criteria now met — ticked in the kanban card with evidence, `readiness.human_approval_recorded` flipped true, `gates.architecture`/`gates.test` flipped true, stage moved implementing -> verify. Regenerating the two kanban HTML embeds hit a `re.subn` gotcha (a plain-string replacement arg lets Python's `re` module interpret `\n`/backslash sequences inside the replacement as regex escapes, silently turning the JSON's escaped newlines into literal ones and corrupting the embedded JS) — caught immediately via a `node -e` parse check, restored both files with `git restore`, and redid the substitution with a lambda replacement (bypasses escape processing) instead; both embeds now parse cleanly and are confirmed byte-identical (24 cards).

## [2026-08-12 03:15] fix | TASK-005.7 verify-stage code review — applied 3 findings + 1 twin bug
- actor: claude-sonnet-5 (via Claude Code)
- files: MCP/Core/Commands/CommandExecutor.RfaFamilyInjection.cs, MCP/Core/Commands/CommandExecutor.GreenMaterial.cs
- trigger: manual (user pasted a code-review report `verify-stage-code-review-2026-08-12.md` covering the `verify`-stage RFA injection work and asked to apply its 3 findings)
- summary: Applied all 3 findings from the pasted review. (1) High: `InjectGreenMaterialIntoFamily`'s final `SaveAs` used `OverwriteExistingFile = true`, but the only safety check (pre/post Type-parameter-signature diff) can't detect geometry/formula/nested-component changes — silently overwriting an existing target `.rfa` could lose those. Fixed by checking `File.Exists(newFamilyPath)` first and throwing (asking the user to change `newFamilySuffix`/`certNo`) instead of overwriting; `OverwriteExistingFile` flipped to `false` as a backstop. (2) Medium: the `finally` block restoring `Application.SharedParametersFilename` only did so `if (!string.IsNullOrEmpty(originalSharedParamFile))`, so a session that started with no shared-param file set would permanently leave Revit's global setting pointed at `GreenMaterial_SharedParams.txt` after the call returned. Fixed to restore unconditionally (`originalSharedParamFile ?? string.Empty`). (3) Medium: `LoadSharedParameters`'s CategorySet-expansion path called `BindingMap.ReInsert()` without checking its bool return value, always incrementing `totalBound` and reporting "已擴充綁定至新品類" even on failure — could surface as `MissingParameters` later during `set_green_material_type_parameters`. Fixed by checking the return value, adding a `totalFailed` counter/status branch, and folding it into the response's `Success`/`Message`. While applying #2, noticed `LoadSharedParameters`'s own shared-param-file restore (same file, ~30 lines above the `GreenMaterial.cs` finding) had the identical bug — not in the report's 3 items, but same root cause, so fixed it too per the same pattern after flagging it to the user and getting a go-ahead. Rebuilt `Release.R24` after each round: 0 errors both times. Not yet deployed/redeployed to a live Revit session this pass.

## [2026-08-12 12:00] docs | Rewrote all docs/green-material/*.md to match current architecture and naming reality
- actor: claude-sonnet-5 (via Claude Code)
- files: docs/green-material/README.md, docs/green-material/Revit_GreenMaterial_Injection_Plan_Specification.md, docs/green-material/revit_injection_logic_and_naming_spec.md, docs/green-material/Revit_Element_GreenMaterial_Mapping_Analysis.md
- trigger: manual (user: "幫我更新docs裡green material的所有文件至最新版", after opening revit_injection_logic_and_naming_spec.md in the IDE)
- summary: Audited all 5 files in docs/green-material/ against ground truth (GreenMaterial_SharedParams.txt's actual 69 PARAM lines across 5 groups, generate_revit_injection_plan.py's real plan dict shape, .claude/skills/import/SKILL.md's actual naming conventions, domain/green-material-parameter-schema.md). Found the docs were a mix of three different kinds of staleness, not a single "outdated" problem, so asked the user (AskUserQuestion) how to handle each before touching anything: full rewrite to current reality, chosen over surgical-patch or list-first options. README.md was updated earlier this session (slash-command table). Revit_Injection_Plan_Report.md was left untouched — confirmed it's an auto-generated snapshot (`generate_revit_injection_plan.py::_write_markdown_report`), not hand-maintained prose, and its "64 個共享參數" phrasing is actually consistent with the Mat1-6 slot-schema subset (excludes Group 4/5), not an error. Fully rewrote Revit_GreenMaterial_Injection_Plan_Specification.md (old version described an unbuilt pyRevit/16-param/45%-75%-ratio design; new version documents the real plan JSON schema, the layerComposition/Set-category-override/wall-usage-matrix/six-slot-assignment alignment chain in priority order, and honestly flags GreenMaterial_DecorArea/QualifyArea/RatioContribution as always-0.0 unimplemented placeholders) and revit_injection_logic_and_naming_spec.md (old version assumed standalone Python injection scripts that were never built and a 31-param schema with un-prefixed field names that don't exist in the real file; new version maps each Scenario to its actual MCP tool — duplicate_element_type/create_single_material_type/create_multi_layer_type/inject_green_material_into_family — with the real naming patterns confirmed against this session's TASK-005.7 live-test output, e.g. `TABC_<SetName>` for combined Types vs `<licno>_<title>` for Scenario 2, and corrected the shared-param table to the real 69/5-group breakdown). For Revit_Element_GreenMaterial_Mapping_Analysis.md — which domain/green-material-parameter-schema.md §2.1 explicitly treats as a historical "early proposal" baseline to contrast against — preserved all original TASK-003 proposal text and extended the existing Scenario-3 strikethrough-plus-correction-note pattern to Scenario 1 (Material layer is single-direction identifier-only, not dual-write; naming is `TABC_<SetName>` not `W_INT_RC15_GBM...`), Scenario 6 (wall thickness comes from user-supplied Q3 free-text keywords, not automatic geometry inference), Scenario 7 (no image-preview base-type matching exists — domain/rfa-family-injection.md rule 1 requires the user to name sourceTypeId explicitly), the §4 master table (added a footnote correcting the reversed Material/Type write-direction and the 16→69 param count), and caught a real internal contradiction in §5 where the "correct naming example" (`GBM0104106_水性漆Finish`) was actually the exact banned example from `.agents/skills/combined-wall-set-import/domain.md`. Verified `.agents/skills/combined-wall-set-import/{SKILL.md,domain.md}` still exist before assuming those links were stale (they're valid, unlike an initial assumption). `verify-qaqc.ps1 -SkipBuild -SkipDeploy`: 54 PASS / 0 FAIL, including link-rot check on the newly added domain/.claude/skills cross-references.
- follow-up not done (flagged to user, out of requested scope): `.claude/skills/import/SKILL.md` line 63 still says "Write the 31 shared parameters" — same stale count as the docs this session fixed, but a currently-active skill file rather than a docs/green-material file.

## [2026-08-12 13:55] feat | TASK-006 rescoped and executed — new green-material-query Skill (57th skill)
- actor: claude-sonnet-5 (via Claude Code)
- files: .claude/skills/green-material-query/SKILL.md (new), tools/kanban/cards/TASK-006.json, kanban.html, tools/kanban/index.html, CLAUDE.md, README.md, README.zh-TW.md, docs/DOCUMENT_AUDIENCE_INVENTORY.md, docs/BIM_MCP/reference/skills-index.html, docs/BIM_MCP/reference/personal-llm-wiki.html, docs/BIM_MCP/index.html
- trigger: manual (user: "請開始執行task006")
- summary: TASK-006's original scope ("撰寫 green-material-takeoff SKILL.md" — 綠建材率檢討 + 計算書匯出) was the same feature the user had just confirmed unnecessary this same session (the Excel/ratio-calc removal from `generate_revit_injection_plan.py`). Flagged the conflict via AskUserQuestion instead of executing blindly; user chose to rewrite the task's scope rather than delete the card or execute as originally written. Second AskUserQuestion narrowed the new scope: a query/inspection Skill for green-material data already written into the model — confirmed via grep that no existing skill covers this (the `/GMimport`→`/import revit` pair only handles writing new data, nothing reads it back out). Built `.claude/skills/green-material-query/SKILL.md`: 5-step workflow (confirm category/scope → list candidate Types via `get_types_by_category`/`list_family_symbols` → read each Type's `GreenMaterial_*` params via `get_element_info` on the TypeId directly, since these are Type-level params → summarize as a table, explicitly barred from computing area/ratio numbers per CLAUDE.md's Tool Call Data Honesty rule → optional color-coding via `query_elements_with_filter`+`override_element_graphics`). Reuses only existing MCP tools, no new tool needed. Updated TASK-006's kanban card (new title, rewritten DOR/DOC, stage ready->implementing, architecture gate flipped true) and regenerated both kanban HTML embeds. This is the project's 57th skill, so followed CLAUDE.md's count-table gate: bumped 56->57 in CLAUDE.md, README.md, README.zh-TW.md, docs/DOCUMENT_AUDIENCE_INVENTORY.md, and every count mention across docs/BIM_MCP/reference/skills-index.html (title/meta/hero/section headers/footer, plus a new skill-card entry in category E and E's subcount 10->11) — `verify-qaqc.ps1` caught two more stale "56" mentions I'd missed on the first pass (`personal-llm-wiki.html` ×2, `BIM_MCP/index.html` hub card), fixed those too. Final `verify-qaqc.ps1 -SkipBuild -SkipDeploy`: 54 PASS / 0 FAIL. Not yet tested against a live Revit connection (kanban card's `test` gate still false).

## [2026-08-12 14:05] test | green-material-query live-tested against real Revit connection, found and fixed a wrong assumption in SKILL.md
- actor: claude-sonnet-5 (via Claude Code)
- files: .claude/skills/green-material-query/SKILL.md, tools/kanban/cards/TASK-006.json, kanban.html, tools/kanban/index.html
- trigger: manual (user: "開始測試", following up on the just-built green-material-query skill)
- summary: Re-anchored the Revit connection (`get_project_info`, same sample project as earlier sessions) then invoked `/green-material-query` with a realistic query ("查詢 Walls 品類裡哪些 Type 已經寫入綠建材資訊") to exercise the skill end-to-end rather than just reading the SKILL.md back. `get_types_by_category(Walls)` returned 13 candidate Types; ran `get_element_info` on all 13 TypeIds. Only `TypeId 263551` ("TABC_室內牆與塗料") came back with any `GreenMaterial_*` fields — full Mat1 (GBM0104088 board) + Mat2 (GBM0104009 paint) data, matching what earlier sessions had written into it. The other 12 (plain RC/light-partition/stacked-wall types) came back with zero `GreenMaterial_*` fields each. This surfaced a real bug in the SKILL.md I'd just written: step 3 claimed a Type with zero `GreenMaterial_*` fields means the whole category was never bound via `load_shared_parameters` — but the live test proved `get_element_info` only returns parameters that actually have a value, so an unfilled Type in an already-bound category looks identical (zero fields) to a genuinely-unbound category. Rewrote that section: can no longer distinguish "category never bound" from "this Type just wasn't filled in" from a single Type's response alone — need to check whether *any* Type in the same batch shows `GreenMaterial_*` fields (proves the category is bound), and only fall back to an idempotent `load_shared_parameters` re-check when the whole batch comes back empty, instead of guessing. Updated TASK-006's `test` gate to true and stage to `verify` with the concrete test evidence in the card; noted the Ceilings/Windows/Doors code paths and the optional color-coding step (5) are still untested. Did not run `verify-qaqc.ps1` this pass — only the SKILL.md body and a kanban card changed, no count-table-affecting files.

## [2026-08-12 15:15] feat | TASK-005.12 — Column/Beam structural material assignment (Scenario 8), built and live-verified
- actor: claude-sonnet-5 (via Claude Code)
- files: assets/green-material-showcase.html, MCP/Core/Commands/CommandExecutor.GreenMaterial.cs, MCP-Server/src/tools/base-tools.ts, .claude/skills/import/SKILL.md, tools/kanban/cards/TASK-005.12.json (new), kanban.html, tools/kanban/index.html
- trigger: manual (user, looking at the Q1/Q2/Q3 alignment modal: "我現在這個Set是混凝土，我想要在set類型新增柱與樑")
- summary: User wanted Column/Beam added to the showcase page's Q2 category options. Investigated the stack first rather than guessing scope: `get_types_by_category` already supports `Columns`/`StructuralFraming`, `duplicate_type_only` is generic (`ElementType.Duplicate()`, not Wall/Floor/Ceiling-specific despite its doc comment), `assign_existing_material` already dispatches to `SetStructuralMaterial` for `FamilySymbol` (confirmed by its own description mentioning columns), and `generate_revit_injection_plan.py` already had `_CATEGORY_HINT_ALIASES`/`_CATEGORY_HINT_FALLBACK` entries for Column/Beam plus a code comment explicitly anticipating "綠混凝土G類" needing Set-level category override — verified against real `tabc_master_database.json` records (231 "混凝土" entries, 209 tagged `綜合建材類` with no Wall-triggering keywords) that this fallback path actually fires correctly, so zero Python changes were needed. The real gap was three pieces: (1) the Q2 modal had no Column/Beam radio options, (2) `load_shared_parameters`'s C# category map (`CommandExecutor.GreenMaterial.cs`) had no `OST_Columns`/`OST_StructuralColumns`/`OST_StructuralFraming` entries — without this, `GreenMaterial_*` params could never bind to a Column/Beam Type and every write would silently land in `MissingParameters` — and (3) no Scenario in `.claude/skills/import/SKILL.md` describing the structural-material-parameter write path (fundamentally different from Wall/Floor/Ceiling's `CompoundStructure` layering: one material, one parameter, no `組合方式`/Q1 concept). Added the two Q2 radios, added a `Columns` special-case in the C# category loop that inserts both `OST_Columns` and `OST_StructuralColumns` into the same `CategorySet` (mirroring `CollectFamilySymbolTypes`'s existing merge for `get_types_by_category`) plus a plain `StructuralFraming` map entry, and wrote Scenario 8 (13 steps: get_types_by_category -> duplicate_type_only -> create_material -> assign_existing_material -> load_shared_parameters -> set_green_material_type_parameters, one Type per material since a structural material parameter can't hold more than one). Rebuilt Release.R24 (0 errors) and MCP-Server (`tsc --noEmit` clean), user closed and reopened Revit for the DLL swap, deployed to `%APPDATA%\Autodesk\Revit\Addins\2024\RevitMCP\`. End-to-end live test used the real `/GMimport` -> `/import revit` flow (not a shortcut): Set 【混凝土_柱】(GBM0104181 綠混凝土G類) correctly resolved to `OST_Columns` via the Set-category-override path exactly as predicted; source `TypeId 257746` (45x60cm 混凝土柱-矩形, 4 existing instances) -> `duplicate_type_only` produced `TypeId 270136` -> `create_material` + `assign_existing_material` (response confirmed `Category: "Structural Columns"`, proving the `FamilySymbol`/`SetStructuralMaterial` branch fired) -> `load_shared_parameters(["Columns"])` returned `TotalBound: 69` across `2 個品類` (confirming the dual-category bind worked) -> `set_green_material_type_parameters` wrote 10/10 fields, 0 missing. `get_element_info` on 270136 confirmed `Structural Material = GBM0104181_綠混凝土G類`, `柱深/柱寬 = 60/45` (geometry intact, `duplicate_type_only` didn't corrupt family parameters), and all `GreenMaterial_Mat1_*` values matched. Cross-checked source `TypeId 257746` unchanged (`Structural Material` still "預設", zero `GreenMaterial_*` fields, 4 instances undisturbed). Created `TASK-005.12` kanban card (stage `verify`, `test` gate true) since this work had no pre-existing card; Beam/`StructuralFraming` shares the same code path as Column but wasn't separately live-tested. `verify-qaqc.ps1 -SkipBuild -SkipDeploy`: 54 PASS / 0 FAIL.

## [2026-08-12 17:05] fix | Beam structural-material write found silently failing on a real family — traced root cause, made the tool report honestly instead of lying
- actor: claude-sonnet-5 (via Claude Code)
- files: MCP/Core/Commands/CommandExecutor.Material.cs, .claude/skills/import/SKILL.md, tools/kanban/cards/TASK-005.12.json, kanban.html, tools/kanban/index.html
- trigger: manual (user: "開始處理", after being told the Beam live test showed assign_existing_material reporting success while the material never actually applied)
- summary: Followed up on the Beam gap flagged in the 15:15 entry — ran the real `/GMimport` -> `/import revit` flow for Set 【混凝土_樑】 (GBM0104181), which correctly resolved to `OST_StructuralFraming` via the Set-category-override path (same mechanism as Column). User picked source `TypeId 209368` (40x80cm 混凝土樑-矩形) from a 2-candidate table; `duplicate_type_only` produced `TypeId 270638`. `assign_existing_material` reported success (`AssignedCount: 1`, `Category: "Structural Framing"`, confirming the `FamilySymbol` dispatch fired), but verification (`get_types_by_category` and `get_element_info`) showed the material was never actually applied — `Materials: ["<none>"]`, no material parameter row at all. First hypothesis: `CollectFamilySymbolTypes` (the read side used by `get_types_by_category`) only fell back from `STRUCTURAL_MATERIAL_PARAM` to `MATERIAL_ID_PARAM` when the former was `null`, while `SetStructuralMaterial` (the write side) also fell back when it was merely read-only — a real inconsistency, fixed by aligning both to the same `null || IsReadOnly` check. Rebuilt, redeployed (user closed/reopened Revit), re-tested — still `<none>`, disproving the read-only theory. Added temporary diagnostics to `SetStructuralMaterial` (split into `SetStructuralMaterialDiagnostic` returning which `BuiltInParameter` was used, its storage type, and whether an immediate in-transaction readback after `.Set()` actually reflects the new value) surfaced in `assign_existing_material`'s response. Rebuilt/redeployed/retested again: diagnostics showed `ParamUsed: "STRUCTURAL_MATERIAL_PARAM"` (present, not read-only — both prior theories wrong) but `VerifiedImmediatelyAfterSet: false` — `.Set()` doesn't throw and doesn't persist, even read back before the transaction commits. This points to a constraint inside the "混凝土樑-矩形" family's own definition (most likely the parameter is associated with a formula or another parameter in the Family Editor), not fixable via the Type-parameter API — Column's family doesn't have this constraint, which is why it worked cleanly the first time. Rather than leave a tool that silently lies about success, changed `SetStructuralMaterialDiagnostic` to throw when the immediate readback doesn't match the intended value, so `AssignExistingMaterial` now correctly reports non-zero `ErrorCount` and excludes the Type from `AssignedTypes`/`successCount` instead of claiming it worked. Rebuilt Release.R24 three times total this session (0 errors each), redeployed twice more (user closed/reopened Revit both times), final re-test confirmed the honest failure: `AssignedCount: 0, ErrorCount: 1`, error message explains the family-level root cause and that opening the Family Editor is the real fix. Added this as a documented Scenario 8 error-handling case in `.claude/skills/import/SKILL.md` (with an instruction to stop before writing `GreenMaterial_*` shared parameters if `assign_existing_material` fails for a Type — the write order already had assign-before-shared-params, so this was just making the stop-on-failure behavior explicit) and updated `TASK-005.12`'s kanban card to record Column as fully verified and Beam as a confirmed family-level limitation (not a Scenario 8 code bug) with the tool now failing loudly instead of silently. `TypeId 270638` is left in a half-built state in the live model (Type + duplicate exist, no material assigned, no `GreenMaterial_*` written since the flow correctly stopped at the failed assign step) — not yet cleaned up, pending the user's call on whether to leave it for manual Family Editor investigation or delete it. `verify-qaqc.ps1 -SkipBuild -SkipDeploy`: 54 PASS / 0 FAIL.

## [2026-08-12 17:10] chore | Deleted the half-built Beam test Type after user confirmation
- actor: claude-sonnet-5 (via Claude Code)
- files: tools/kanban/cards/TASK-005.12.json, kanban.html, tools/kanban/index.html
- trigger: manual (user picked "刪除" via AskUserQuestion)
- summary: `TypeId 270638` (the Beam Type left over from the failed structural-material assignment) had no material and no `GreenMaterial_*` params written, so nothing green-material-related was lost by removing it. Called `delete_element(270638)`, confirmed via `get_types_by_category(StructuralFraming)` the category is back to its original 3 Types. Noted the cleanup in TASK-005.12's card and regenerated both kanban embeds.

## [2026-08-12 17:30] feat | Beam scenario fully verified after user fixed the family's parameter scope
- actor: claude-sonnet-5 (via Claude Code)
- files: tools/kanban/cards/TASK-005.12.json, kanban.html, tools/kanban/index.html
- trigger: manual (user opened the "混凝土樑-矩形" family themselves, unchecked the Instance box on the Structural Material parameter in Family Types, reloaded into the project, then said "我替換成type層級了")
- summary: User's own diagnosis (noticed the Material parameter was Instance-scoped in the Family Editor) matched exactly what the earlier diagnostic run had shown indirectly. Explained why that's family-authoring-specific, not a StructuralFraming category limitation, and walked through the Family Types dialog steps to convert it (safe here since both existing Types in that family had 0 instances, so no per-instance data would be lost). After the user made the change and reloaded, `get_types_by_category(StructuralFraming)` confirmed `TypeId 209368` now shows a real Type-level material ("混凝土 - 現場澆注混凝土"). Reran Scenario 8 end-to-end from that source: `duplicate_type_only` -> `TypeId 272397`, `assign_existing_material` -> `VerifiedImmediatelyAfterSet: true` (the diagnostic field added during the earlier investigation now confirms success instead of a silent no-op), `load_shared_parameters` skipped 69/69 as already-bound (idempotent), `set_green_material_type_parameters` wrote 10/10 with 0 missing. `get_element_info` confirmed `Structural Material = GBM0104181_綠混凝土G類`, `樑深/樑寬 = 80/40` matching source geometry, and all `GreenMaterial_Mat1_*` fields correct. `write_back_to_set_manager` marked Set 【混凝土_樑】 as complete. Updated `TASK-005.12`'s test_passed line to reflect both Column and Beam now fully verified — the earlier Beam failure was conclusively a single family's author-time parameter-scope choice, not a Scenario 8 or category-wide limitation. `verify-qaqc.ps1 -SkipBuild -SkipDeploy`: 54 PASS / 0 FAIL.

## [2026-08-12 17:36] docs | Added L-031 lesson (Set() success ≠ actually persisted) and linked it from the import skill
- actor: claude-sonnet-5 (via Claude Code)
- files: domain/lessons.md, .claude/skills/import/SKILL.md
- trigger: manual (user asked "在SET即將寫入REVIT元件時，發現TYPE裡沒有對應的參數，agent會怎麼做" then confirmed "好" to formalizing it as a lesson)
- summary: Explained the two distinct failure modes covered this session (unbound shared-parameter category -> honest MissingParameters; Type-level write that reports success but doesn't persist -> now caught via immediate in-transaction readback and reported as a real error) as direct answers, then captured the generalizable principle as `domain/lessons.md` L-031 via the `/lessons` skill: `Parameter.Set()` not throwing is not proof of persistence, especially when a family author scoped a parameter to Instance rather than Type, or associated it with a formula — any Type-level write tool needs an immediate readback check, not just absence-of-exception. Added a "## Lessons Reference" section to `.claude/skills/import/SKILL.md` pointing at L-031, matching the pattern other skills (e.g. element-query -> L-001) already use. `verify-qaqc.ps1 -SkipBuild -SkipDeploy`: 54 PASS / 0 FAIL both times (after the lessons.md edit and again after the skill cross-reference).

## [2026-08-12 17:45] chore | TASK-008 (DOD 總驗證卡) status refresh to match actual sub-card state
- actor: claude-sonnet-5 (via Claude Code)
- files: tools/kanban/cards/TASK-008.json, kanban.html, tools/kanban/index.html
- trigger: manual (user: "請執行task008")
- summary: TASK-008 is the umbrella DOD-verification card for the 10 green-material import scenarios (TASK-005.1~005.11 minus the removed 005.9). Its checklist was last edited 2026-08-11 and claimed only 3/10 scenarios fully passed; checking every TASK-005.x card's actual `stage`/`gates` today showed all 10 original scenario sub-cards (005.1, 005.2, 005.3, 005.4, 005.5, 005.6, 005.7, 005.8, 005.10, 005.11) are `stage: done` with all six gates `true` — the rest were completed in sessions between 08-11 and 08-12 but TASK-008's own card was never synced. There is also a bonus TASK-005.12 (Column/Beam structural-material injection, added 2026-08-12, not one of the original 10 scenarios) that is end-to-end verified per today's log entries but still sits at `stage: verify` pending its own `code_review` sign-off. Rewrote TASK-008's content to list all 10 scenarios as done with their evidence, flipped its `architecture` and `test` gates to `true` (both objectively satisfied: every scenario uses the proper MCP-tool + C#-dispatch path, and every scenario has logged end-to-end verification), moved its `stage` from `implementing` to `verify` (matching the 005.12 pattern), and left `code_review` `false` since that gate explicitly requires the user's own final sign-off — TASK-008 cannot self-approve that step. Updated the same embedded `cardsData` block in both `kanban.html` and `tools/kanban/index.html` to match. `verify-qaqc.ps1 -SkipBuild -SkipDeploy`: 54 PASS / 0 FAIL.

## [2026-08-12 17:58] chore | TASK-007 deleted — user already contributed the green-material tooling to HJPLUS knowledge base directly, and it was merged
- actor: claude-sonnet-5 (via Claude Code)
- files: tools/kanban/cards/TASK-007.json (deleted), tools/kanban/cards/TASK-008.json, kanban.html, tools/kanban/index.html
- trigger: manual (user: "我自己已經有把前部分綠建材TOOL功能寫入HJPLUS 知識庫了，並且已經被MERGE進去了，task007可以刪除")
- summary: TASK-007 tracked running the `hj-pr-proposal` skill to translate the green-material domain/skill content into an HJPLUS-format PR draft (domain.md + SKILL.md). User reports they already did this contribution manually and it has been merged upstream, so the card is obsolete. Deleted `tools/kanban/cards/TASK-007.json`; confirmed via grep it was the only other card referencing TASK-007 was TASK-008 (via `dependsOn`), so cleared that dangling reference to `[]` in TASK-008.json. Removed the matching TASK-007 card object from the embedded `cardsData` array in both `kanban.html` and `tools/kanban/index.html`, and synced the same `dependsOn` cleanup there. Verified both HTML embeds still parse as valid JSON (23 cards each, TASK-007 absent, TASK-008 present) after the edit. Note: TASK-008 was separately updated to `stage: done` / `gates.code_review: true` directly by the user in this session (not by this change). `verify-qaqc.ps1 -SkipBuild -SkipDeploy`: 54 PASS / 0 FAIL.

## [2026-08-12 18:26] docs | Named the green-material tool "GreenMaterial Injector" and wrote its user guide
- actor: claude-sonnet-5 (via Claude Code)
- files: docs/green-material/GreenMaterial_Injector_User_Guide.md (new), docs/green-material/README.md
- trigger: manual (user asked to name the now-complete green-material MCP tool suite; after picking "GreenMat Injector" direction via AskUserQuestion, user said to finalize as "GreenMaterial Injector" and write a usage manual, filed per this project's own document-classification rules)
- summary: Per `docs/DOCUMENT_AUDIENCE_INVENTORY.md`, `docs/green-material/**` is already the classification bucket for "green-material developers" audience docs (architecture, mapping, naming, injection-plan docs), so the new user guide was placed there rather than under `domain/` (SOP) or `.claude/skills/` (AI orchestration) — it's an operational how-to, not either of those. Wrote `GreenMaterial_Injector_User_Guide.md`: what the tool is and why the name fits (traces directly to existing code/terminology — `inject_green_material_into_family` tool, "Injection Plan" artifact, `GreenMaterial_*` shared-parameter prefix, not an invented name), the 3-layer MCP architecture prerequisites checklist (Revit running, add-in enabled, port 8964, single-connection limitation, DLL redeploy needs a Revit restart), the 5-command workflow table (`/GMupdate` -> `/GMweb open` -> `/GMimport` -> `/import revit` -> `/GMset compare`, plus the natural-language `green-material-query` skill), a walkthrough example explicitly labeled as illustrative (not live tool output, to respect the "tool call data honesty" guard rail), a table of all 11 verified import scenarios (sourced from the actual `TASK-005.1`~`TASK-005.11` kanban card titles plus the bonus `TASK-005.12` Column/Beam scenario, including the L-031 Instance-vs-Type-parameter gotcha), and a troubleshooting table covering the failure modes actually documented in this session's and prior sessions' log entries (port stuck, single-connection replacement, stale DLL, `Set()` succeeding without persisting). Deliberately did not hardcode the live tool/domain/skill counts (178/76/57) since this file isn't part of QA/QC's count-sync watch list (`CLAUDE.md`/README/README.zh-TW/audience-inventory only) and a hardcoded number here would silently rot; referred readers to `CLAUDE.md` instead. Updated `docs/green-material/README.md`'s index table to add the new guide as the first entry and stated the product name "GreenMaterial Injector（綠材注入器）" in a blockquote under the H1 so the name is discoverable from the folder's existing entry point. Did not rename anything else (e.g. the showcase page's own `<title>`, which describes that one component — the search platform — not the whole tool) since the user's ask was scoped to the tool's title + a new manual, not a full rebrand pass. `verify-qaqc.ps1 -SkipBuild -SkipDeploy`: 54 PASS / 0 FAIL.

## [2026-08-12 18:44] docs | Merged the standalone user guide into docs/green-material/README.md to remove duplication
- actor: claude-sonnet-5 (via Claude Code)
- files: docs/green-material/README.md, docs/green-material/GreenMaterial_Injector_User_Guide.md (deleted)
- trigger: manual (user, having opened README.md in the IDE: "README跟使用說明書重工了，把使用說明書整合進README")
- summary: The previous turn's new `GreenMaterial_Injector_User_Guide.md` duplicated content already in `README.md` (the slash-command table and typical-flow line existed in both, just phrased differently). Folded the guide's full content into `README.md` in place — architecture/prerequisites, the 5-command workflow table (now the single authoritative version, richer than the old README table), the illustrative walkthrough example, the 11-scenario support table, and the troubleshooting table — under a new top-level structure with the H1 renamed to the product name "GreenMaterial Injector（綠材注入器）" so the file now serves as both the folder's entry point and its usage manual. Consolidated the old flat "文件索引" table and the guide's "相關文件" list into one "文件索引" section split into 本資料夾內文件 (dev/spec docs) and 權威 SOP 與 AI 操作規範 (domain files, skills, CLAUDE.md pointer), keeping the archive-index and domain-authority pointers at the bottom. Deleted `GreenMaterial_Injector_User_Guide.md` since its content now lives in README.md and having both was the duplication the user flagged. Confirmed no other file references the deleted path (only this append-only log's prior entry does, left untouched). `verify-qaqc.ps1 -SkipBuild -SkipDeploy`: 54 PASS / 0 FAIL.
## [2026-08-12 18:52] docs | Reorganized the green-material README around the user journey
- actor: codex (via Codex)
- files: docs/green-material/README.md
- trigger: manual
- summary: Reframed the GreenMaterial Injector README for first-time users: led with outcomes and the Revit-write safety boundary, reduced the core path to select materials -> review an injection plan -> confirm the Revit write, separated daily commands from data-maintenance commands, added a first-run example and pre-write checklist, simplified the support matrix, moved architecture and developer-facing references later, and converted key domain references to clickable links. Preserved the existing feature coverage and limitations. `verify-qaqc.ps1 -SkipBuild -SkipDeploy`: 54 PASS / 0 FAIL.

## [2026-08-12 19:46] refactor | Prefixed all GreenMaterial Injector domain/skill/program files with `GM_`
- actor: claude-sonnet-5 (via Claude Code)
- files: 7 `.claude/skills/*` dirs renamed (GM->GM_kanban, GMimport->GM_import, GMset->GM_set, GMupdate->GM_update, GMweb->GM_web, green-material-query->GM_query, import->GM_inject); 4 `domain/*.md` renamed (green-material-catalog.md->GM_catalog.md, green-material-parameter-schema.md->GM_parameter-schema.md, green-material-keyword-search.md->GM_keyword-search.md, rfa-family-injection.md->GM_rfa-family-injection.md); 2 `MCP/Core/Commands/CommandExecutor.*.cs` renamed (GreenMaterial->GM_GreenMaterial, RfaFamilyInjection->GM_RfaFamilyInjection); 4 root Python scripts renamed (generate_revit_injection_plan.py, apply_revit_injection_plan.py, update_tabc_database.py, validate_shared_params.py, all GM_-prefixed); plus reference fixes in CLAUDE.md, MCP-Server/src/tools/visualization-tools.ts, docs/green-material/*.md (5 files), tools/green-material/README.md, docs/BIM_MCP/reference/{skills-index,domain-index}.html, assets/green-material-showcase.html, .claude/settings.local.json, .gitignore
- trigger: manual (user request)
- summary: Renamed every live file in the GreenMaterial Injector subsystem (skills, domain SOPs, C# command modules, Python engine scripts) to carry a `GM_` prefix, per user request, using `git mv` to preserve history (plain filesystem move for two files that were still untracked). Updated every cross-reference this touched — skill frontmatter `name:` fields and slash-command text, CLAUDE.md's domain trigger table, domain files' mutual `related:` links, C#/TS doc-comment paths, Python self-referential comments, the production `assets/green-material-showcase.html` page's ~13 hardcoded `/GMimport` copy-paste command strings (user-facing, easy to miss), the two BIM_MCP HTML reference indexes, and local Bash-permission allowlist strings. Deliberately left untouched (per explicit user scope decisions): data/output files (GreenMaterial_SharedParams.txt, exported_material_sets.json, tabc_master_database.json, Revit_Injection_Plan.json), MCP tool identifiers and C# `case` strings (e.g. `create_green_material`), `docs/green-material/*.md` filenames themselves (content links inside them were still fixed), `tools/green-material/archive/**`, and append-only history (log/*.md, kanban.html/tools/kanban/**, ai/artifacts/**, docs/tabc_search.html). Full-repo grep sweep confirmed no live stale references remain outside the excluded paths. `verify-qaqc.ps1 -SkipBuild -SkipDeploy`: 54 PASS / 0 FAIL.
- **follow-up (same session, user-caught gap)**: the initial sweep's grep patterns for `assets/green-material-showcase.html`, `GM_generate_revit_injection_plan.py`, `domain/GM_parameter-schema.md`, and `docs/green-material/revit_injection_logic_and_naming_spec.md` never included the bare phrase `/import revit` (only `/GMimport` was checked), so the old `import` skill's rename to `GM_inject` was left half-applied in those four files — including the showcase page's own "複製導入指令 (/import revit)" button and copy-to-clipboard text, the user-facing half of the `/GM_import` flow. Also missed: `.agents/skills/combined-wall-set-import/SKILL.md`, a separate skill outside `.claude/skills/` that referenced `/import revit` in its trigger condition and was never in the original file inventory. All five fixed; re-ran `verify-qaqc.ps1 -SkipBuild -SkipDeploy`: still 54 PASS / 0 FAIL.

## [2026-08-12 20:09] chore | Untracked churn-only injection-plan snapshots, archived superseded TASK-003 analysis doc
- actor: claude-sonnet-5 (via Claude Code)
- files: .gitignore, docs/green-material/README.md, domain/GM_parameter-schema.md, domain/GM_rfa-family-injection.md, tools/green-material/README.md; deleted from git tracking (kept on disk): Revit_Injection_Plan.json, docs/green-material/Revit_Injection_Plan_Report.md; renamed: docs/green-material/Revit_Element_GreenMaterial_Mapping_Analysis.md -> tools/green-material/archive/reports/Revit_Element_GreenMaterial_Mapping_Analysis.md
- trigger: manual (user asked whether any GreenMaterial Injector files/docs were safe to delete/retire)
- summary: Audited the whole GreenMaterial Injector subsystem for dead weight. `Revit_Injection_Plan.json` and its human-readable twin `docs/green-material/Revit_Injection_Plan_Report.md` are single-slot "most recent run" scratch outputs that `/GM_import`/`/GM_set compare` fully overwrite every execution (both already spec'd as "自動產生，勿手動編輯" in `Revit_GreenMaterial_Injection_Plan_Specification.md`); each had accumulated 8 no-value churn commits, so gitignored them the same way `tabc_master_database.json`/`assets/green-material-showcase.html` already are (third-party-data precedent, same section extended) — functionality unaffected since the generating scripts/skills still read/write the same on-disk paths, only git stops tracking. `Revit_Element_GreenMaterial_Mapping_Analysis.md` self-identifies in its own header as the TASK-003-era early proposal, superseded by `domain/GM_parameter-schema.md` 2.1 and `GM_rfa-family-injection.md`; its own text asks that the proposal wording be kept (not deleted) for traceability, so moved it into `tools/green-material/archive/reports/` (the directory tools/green-material/README.md already designates for "開發期間的文字分析結果") rather than deleting, and fixed its two inbound path references plus its README.md table row. `exported_material_sets.json` was confirmed as genuine accumulating shared state (not transient) and left tracked. `verify-qaqc.ps1 -SkipBuild -SkipDeploy`: 54 PASS / 0 FAIL.

## [2026-08-13 16:01] docs | Rebuilt GreenMaterial Injector README as a Quick Start
- actor: codex (via Codex)
- files: docs/green-material/README.md
- trigger: manual
- summary: Rebuilt the README around one continuous first-use path: environment check, material selection, plan review, confirmed Revit write, and result verification. Added a copy-ready first prompt, explicit AI Client/Skill prerequisites, expected outputs, a compact command table, and user-actionable limitations; moved architecture and internal history out of the main path and retained links to authoritative technical documents. No suitable product screenshots currently exist in the repository, so no placeholders or fabricated UI images were added. Preserved the concurrently updated GM_query Column/Beam behavior. `verify-qaqc.ps1 -SkipBuild -SkipDeploy`: 54 PASS / 0 FAIL.

## [2026-08-13 16:20] chore | Archived the last stray one-off script from repo root
- actor: claude-sonnet-5 (via Claude Code)
- files: add_reset_func.py -> tools/green-material/archive/scripts/maintenance/add_reset_func.py, tools/green-material/README.md
- trigger: manual (user asked to archive green-material-related program files)
- summary: Audited every tracked green-material-related file for anything outside the established `tools/green-material/README.md` classification. All root-level Python entry points, shared-param/state files, and `docs/tabc_search.html` (deliberately kept as append-only history per the 2026-08-12 GM_ rename log entry) were already accounted for; the one outlier was `add_reset_func.py`, a one-off script that patched `kanban.html`/`tools/kanban/index.html` to add `resetKanbanState()` — confirmed both targets already contain the function, so the patch already ran and the script was dead weight in root. `git mv`'d it into `archive/scripts/maintenance/` (same category as `update_kanban_cards.py`) and noted it in the README's directory-classification table.

## [2026-08-13 16:45] refactor | Moved the 5 official GreenMaterial Injector Python entry points from repo root into tools/green-material/
- actor: claude-sonnet-5 (via Claude Code)
- files: `git mv` GM_apply_revit_injection_plan.py, GM_generate_revit_injection_plan.py, GM_update_tabc_database.py, GM_validate_shared_params.py, GreenMaterial_SharedParams.txt into `tools/green-material/`; path-logic fixes in the two moved scripts; `.claude/skills/{GM_import,GM_inject,GM_update,GM_set}/SKILL.md`; `MCP-Server/src/tools/visualization-tools.ts`; `docs/green-material/revit_injection_logic_and_naming_spec.md`; `tools/green-material/README.md`
- trigger: manual (user follow-up after the add_reset_func.py archive: "這邊綠建材相關的檔案沒有歸到檔", asked to move the remaining root-level GreenMaterial Injector files into tools/green-material/, confirmed via AskUserQuestion)
- summary: The user wanted the 5 previously-root-level "正式來源" Python/txt files (the plan engine, TABC updater, injection stub, shared-param validator, and the shared-parameter schema file itself) relocated into `tools/green-material/` proper (not `archive/`) rather than kept at repository root. Two of the five (`GM_update_tabc_database.py`, `GM_generate_revit_injection_plan.py`) compute a `WORKSPACE` constant via `os.path.dirname(os.path.abspath(__file__))` to locate sibling data files that **stay at repo root** (`tabc_master_database.json`, `exported_material_sets.json`, `Revit_Injection_Plan.json`, `assets/green-material-showcase.html`) — moving the scripts one directory deeper without fixing this would have silently broken every data read/write (paths would have resolved to a nonexistent `tools/green-material/tabc_master_database.json` etc.), so both were changed to `SCRIPT_DIR = os.path.dirname(os.path.abspath(__file__))` + `WORKSPACE = os.path.dirname(os.path.dirname(SCRIPT_DIR))`, verified against the real files afterward (`WORKSPACE` now correctly resolves back to repo root, `DB_PATH`/`SETS_FILE`/`SHOWCASE_PATH` all exist). `GM_validate_shared_params.py` needed no change — `GreenMaterial_SharedParams.txt` moved into the same directory as the script. `GM_apply_revit_injection_plan.py` has no path logic. Every SKILL.md `python -c "import GM_generate_revit_injection_plan as g"` block (9 occurrences across GM_import/GM_inject/GM_set) got `import sys; sys.path.insert(0, 'tools/green-material')` inserted before the import (scripted via a Python regex pass per file, not manual edits, to guarantee identical treatment across all occurrences); GM_update's two direct `python GM_update_tabc_database.py` invocations became `python tools/green-material/GM_update_tabc_database.py`. Also fixed every prose/doc-comment claim of "repo root" location for these 5 files across the 4 skill files, `tools/green-material/README.md`'s own source-of-truth table and archival rules, `docs/green-material/revit_injection_logic_and_naming_spec.md`'s relative markdown link, and one `MCP-Server/src/tools/visualization-tools.ts` tool-schema description; along the way fixed an unrelated pre-existing stale reference in the same TS file (`generate_revit_injection_plan.py` missing its `GM_` prefix from the 2026-08-12 rename). Deliberately left unchanged (no location claim to fix): `domain/GM_*.md`, `MCP/Core/Commands/CommandExecutor.GM_*.cs`, `docs/BIM_MCP/reference/skills-index.html`, `.agents/skills/combined-wall-set-import/SKILL.md` — all reference these filenames generically without asserting a directory. `.gitignore`'s `/tabc_master_database.json`/`/assets/green-material-showcase.html`/`/Revit_Injection_Plan.json`/`/docs/green-material/Revit_Injection_Plan_Report.md` patterns needed no change since those sibling files stayed at repo root. Verified end-to-end with a real smoke test: `sys.path.insert(0, 'tools/green-material'); import GM_generate_revit_injection_plan as g; g.generate_injection_plan(...)` successfully matched a real licno and wrote a plan. `verify-qaqc.ps1 -SkipBuild -SkipDeploy` run after.

## [2026-08-13 17:05] refactor | Moved kanban.html into tools/green-material/ as GM_kanban.html, fixed its File-System-Access write-back path lookup
- actor: claude-sonnet-5 (via Claude Code)
- files: `git mv` kanban.html -> tools/green-material/GM_kanban.html; `.claude/skills/GM_kanban/SKILL.md` (rewritten); `tools/kanban/index.html` (mirror script) and `tools/green-material/GM_kanban.html` itself (both embed an identical copy of the same script); `docs/BIM_MCP/reference/skills-index.html`; `.agents/skills/monstrare_task_verify/SKILL.md`; `tools/green-material/README.md`
- trigger: manual (user opened kanban.html and asked to archive it into green-material with a GM_ prefix)
- summary: Unlike the previous Python-script move, `kanban.html` had a real functional coupling beyond documentation: `tools/kanban/index.html`'s in-page "🔗 連結專案資料夾" (File System Access API) write-back feature hardcoded `repoRootHandle.getFileHandle('kanban.html')` twice (a connect-time sanity check and the actual save-time write target) to locate the file at repo root and rewrite its embedded `cardsData` block — a rename/move without fixing this would have silently broken that button (file-not-found on save). Flagged this to the user via AskUserQuestion before acting, since it directly contradicted the 2026-08-12 GM_ rename's explicit decision to leave `kanban.html`/`tools/kanban/**` untouched as "append-only history"; user confirmed: move it and fix the JS. Added a shared `getGmKanbanFileHandle(rootHandle)` helper (3-level `getDirectoryHandle('tools')` -> `getDirectoryHandle('green-material')` -> `getFileHandle('GM_kanban.html')`) and used it at both call sites, replacing the flat `getFileHandle('kanban.html')` calls; updated the alert/comment strings that named the old path too. Critically, **both** `tools/kanban/index.html` and `tools/green-material/GM_kanban.html` needed this identical fix — they're mirror copies embedding the exact same script (regenerated from `tools/kanban/cards/*.json`), so `tools/green-material/GM_kanban.html`'s own copy of the write-back code was carrying the same stale self-reference; missing that would have left the file that moved unable to find itself. Verified both embedded `<script>` blocks are syntactically valid (`node --check`) and byte-identical to each other after the edit (`diff` over the edited line range). Rewrote `.claude/skills/GM_kanban/SKILL.md` end-to-end for the new path (description, Usage, Steps, error-handling table) and added a note cross-referencing the new `getGmKanbanFileHandle()` dependency so a future move doesn't repeat this gap. Left `tools/kanban/index.html`'s own filename/location, `tools/kanban/cards/*.json`, and `tools/kanban/epics.json` untouched — user's request was scoped to `kanban.html` only. `verify-qaqc.ps1 -SkipBuild -SkipDeploy` run after.
